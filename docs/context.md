# context.md

Session history, lessons learned, and ruled-out dead ends for this project. `docs/ARCHITECTURE.md`
covers current architecture and how to work in the repo; this file covers *why* things
are the way they are and what's already been tried and rejected, so it doesn't get
re-litigated. Read both — docs/ARCHITECTURE.md for the current state of truth, this for the journey.

## The arc so far

Started as a proof-of-concept console app (`desflver_test/`, since deleted) that used
WitchyBND + the vendored SoulsFormatsNEXT library to write `.obj`/`.mtl`/`.png` for
Godot's generic Wavefront OBJ importer to pick up — fought constantly (path leakage,
raw DDS bytes, a Godot OBJ/PNG-import race bug) before being replaced entirely by a
native `EditorSceneFormatImporter` (itself later replaced by the current architecture,
see below). Two facts from this era still matter: **no Godot build has C# support except
the official godotengine.org `.NET` build** — confirmed the hard way on Redot (none at
all) and Steam's Godot (also none) — and **a forked/patched vendored submodule's local
commits are a live risk until actually pushed to a real remote**, not just a tidiness
concern: `SoulsFormatsNEXT`'s two local patches sat as uncommitted working-tree edits in
a plain clone for a while, meaning any `git pull`/`checkout` in that directory would have
silently wiped them with no recovery path, before being committed to a real fork and
pulled back in as a proper submodule. The project was also renamed `Boletaria` →
`Soulbrandt` along the way and made public under GPLv3 (required by `SoulsFormatsNEXT`'s
own license, not an arbitrary choice).

## Real bugs found and fixed (root causes, not symptoms)

Each of these produced a plausible-looking but wrong intermediate theory before the real
cause was confirmed — noted so the same wrong turn isn't re-taken.

- **Five foundational bugs from the pre-architecture-pivot era, all now standing conventions described in docs/ARCHITECTURE.md — kept here only as a "why," not the full narrative.** Pfim's decoded buffer holds the full mip chain, not just the base level — must slice to `width*height*4` before `Image.CreateFromData` (confirmed via `pfImage.MipMaps[]`'s real per-level offsets). Godot uses clockwise front-face winding with no UV V-flip needed; the old `desflver_test/` OBJ-export tool's winding-swap/V-flip logic was specific to Blender/OBJ conventions and wrong to port into the native path (confirmed via 16k+ triangle normal comparison and a wireframe-vs-texture visual match). `SoulsFormatsNEXT`'s own `Mesh.cs` picks a UV scale factor by header version (`>= 0x12` → 2048 else 1024) — wrong for DeS, which always needs `1024` regardless of version (confirmed via UV-wireframe overlay on two models with different header versions); patched directly in the vendored fork. **The big one, went unnoticed for a long time**: every import was a left/right mirror of the real thing, invisible because mirroring one axis also flips the apparent triangle winding — the existing winding/UV checks kept passing on a fully-mirrored-but-internally-consistent import, and a bilaterally-symmetric human skeleton doesn't reveal it either. Caught only because the user recognized a real map (`m03_01_00_00`) as a literal mirror of its sibling, then confirmed via `c9990`'s swapped shield/sword hands; root-caused two independent ways (skeleton bind-pose FK, and the map-level mirror itself). Fix: negate X on position/normal, swap two triangle indices per face (required *because of* the X negation, not a separate correction). Verified: reimporting the full corpus produced the same known-error count, and the user visually confirmed both cases.
- **Two empty-imported-scene bugs, one shared root: `ImporterMesh`/`ImporterMeshInstance3D` never render outside Godot's own import pipeline, and `PackedScene.pack()` only serializes descendants with `.Owner` set.** Both are still-live requirements, documented as current architecture in docs/ARCHITECTURE.md. Diagnostic trick worth keeping: `ResourceSaver.save(packed, "res://debug.tscn")` dumps a resource as human-readable text.
- **Case-sensitive texture lookup and a race condition in an old area-texture cache — both closed.** Texture-name dictionaries are `OrdinalIgnoreCase` throughout (a material referenced `m03_01_wall_cliff_00`, the real TPF entry was `m03_01_Wall_Cliff_00`). The cache race was a non-thread-safe `Dictionary` under Godot's old multi-threaded reimport dispatch — can't recur, `FlverLoader` is single-threaded with no such dispatch anymore. Worth remembering the diagnostic signature: inconsistent, non-reproducible-the-same-way symptoms mean a threading bug, not a logic bug.
- **Alpha/cutout MTD-suffix convention (`_Edge`/`_Alp`/`_Add`) and map texture resolution (same-basename sibling vs. shared per-area folder) — both fully described as current behavior in docs/ARCHITECTURE.md.** `_Add` was a later addition (found via `m8003b1`/`m3051b0`'s missing volumetric-light blending, confirmed real via a 22-material/15-file corpus scan, never used on solid geometry).
- **Terrain ground-blend layer (hard seams between grass/dirt/stone) — fixed, fully described in docs/ARCHITECTURE.md's terrain-blend entry.** Gated on FromSoft's own `[M]`/`[ML]` MTD bracket tag, not `g_Specular_2`/`g_Bumpmap_2` presence (tried first, wrongly excluded real materials missing an optional specular layer — confirmed via a 963-material scan). Blend weight is `VertexColor.A`, not `.R`/`.G`/`.B` (confirmed via corpus correlation stats, not just "varies 0-1" — R turned out to be a binary per-vertex flag when tried as AO and visually disproven; what R actually encodes is still unknown — don't re-try "resembles an AO shape" as confirmation without checking for continuous values first).
- **Water surfaces and cross-category texture reuse — both fully superseded/described elsewhere, kept only for what isn't duplicated.** Water: `g_Envmap` uniquely gates the family (confirmed via direct scan, not a filename heuristic); `g_WaterColor`'s alpha channel was initially silently discarded, which is what actually separates murky from clear water — see the interop gotchas section below for the real `SetShaderParameter`/`source_color` bug found while wiring this up. Cross-category reuse (a corpse pile rendering fully white, reusing a *character* model's texture set) is now handled by `CandidateDirs` — see the "Texture-resolution redundancy" entry below.
- **Map-piece naming investigation (`l1` suffix, Boletarian Palace) — curiosity-driven, findings scoped into docs/PLAN.md's map-assembler item.** `l1` is a map-piece-only convention, unrelated to parts' `_L` LOD suffix (confirmed: `l1` appears nowhere else in the ~1030-file map dataset, no `l2`/`l3` anywhere). Geometry comparison mostly but not cleanly supports "reduced-detail variant" (5/8 have fewer verts at the same bounding box, but one has identical counts and another has nearly double). The white patches on 3 of these files are unrelated and fully root-caused: all reference `m02_tower_wall_plain.tga`, which doesn't exist anywhere in the extracted dataset — genuinely orphaned source data, not an importer bug. Tested "used in the opening cutscene" against real placement data and found no support: a crude but effective diagnostic (raw string-count of each model name in the `.msb`) showed every ordinary placed piece scoring exactly 3 occurrences, all 8 `l1` files scoring 0 — more likely intended-but-cut content than cutscene-specific. **Bigger-picture lesson, confirmed not assumed: not every loose FLVER sitting in a map's folder is actually placed in that map** — any future map assembler needs to consult the MSB's own Parts list, not just glob the directory.
- **Origin-offset decoration sub-meshes — investigation only, conclusion now in docs/ARCHITECTURE.md's coordinate-conventions section.** Confirmed real and general (not a one-off): several map pieces (`m2501b0`, `m2210b0`, `m2601b0`, `m4013b0`) have a sub-mesh sitting at local origin instead of its real position, alongside a `FLVER.Node` whose name and `Translation` match where it should go — but `Mesh.NodeIndex` (the one field that could link them) is uniformly `0` everywhere checked, so it's not a live, importer-readable binding. Best-supported explanation: each decoration gets its own MSB placement entry referencing this shared FLVER plus a specific node — another confirmation that real map assembly needs MSB parsing, not just FLVER import.
- **Asset mounting system built — WitchyBND no longer needed, fully described as current behavior in docs/ARCHITECTURE.md's "Asset mounting" section.** Confirmed `SoulsFormatsNEXT`'s `DCX`/`BND3`/`BND4` readers handle this game's real containers directly (`DCX_EDGE` compression, confirmed via raw header bytes and the library's own `Type.DemonsSouls` alias; no external name-dictionary needed, unlike later-game hashed-name formats). One real interop gotcha: BND entry names use a `DVDROOT` path segment, distinct from `Model` (what FLVER texture *references* use) — easy to conflate, handled by dropping exactly one segment after `data` regardless of which literal word it is.

- **Non-visual game-logic data investigation (curiosity-driven, not a bug report —
  headline findings already condensed into docs/PLAN.md's Phase 2 section; kept here for
  what isn't duplicated there).** Confirmed directly against the real mounted dump, not
  assumed from general Souls-series knowledge, that none of Phase 2's eventual
  gameplay-logic needs would require any DRM circumvention: `EBOOT.BIN` (Sony's encrypted
  SELF container) is correctly off-limits, but `script/` (plain unencrypted Lua, same
  approach as DS1), `param`/`paramdef` (DeS ships its own paramdefs, unlike later titles
  needing the community `Paramdex` project), and per-character `.tae`/`.esd` files are
  all real, already-parseable, disc-native data. One naming trap worth remembering: DeS's
  poise-like mechanic is internally called **Super Armor**, not Poise — there's no
  DS1-style "Poise stat reduces hitstun" field in any table checked, so porting fan
  terminology directly would be wrong. Two real gaps remain: TAE event `Type` IDs have no
  friendly name without an external template (unbuilt for DeS), and precise i-frame/
  hyper-armor frame counts aren't publicly documented anywhere for this specific title
  (unlike Dark Souls 3) — likely needs direct frame-by-frame observation against the real
  game in RPCS3, not extraction.

- **Texture-resolution redundancy found and unified (`GetAreaTextures` deleted) — the audit and resulting `CandidateDirs` architecture are now fully described in docs/ARCHITECTURE.md's "Texture resolution" section, not duplicated here.** Triggered by asking directly whether the accumulated per-case methods were actually clean or "smaller solutions taped together" — checked via a 26,656-ref corpus scan rather than assumed: `GetAreaTextures` never once succeeded where `GetForeignCategoryTextures` didn't already cover it. Replaced both (plus the about-to-be-added obj/parts sib/tex rule) with one ordered `CandidateDirs` chain. One case deliberately **not** generalized into a fifth rule at the time: `o6510_1`'s bottle/vase texture (byte-identical across 6 unrelated obj containers, not in its own folder or any map bucket) — no cheap signal for which sibling folders to search, flagged as needing more real examples before it's worth the scan-cost/collision-risk tradeoff (later resolved by `SiblingObjTextureDir`, see docs/ARCHITECTURE.md).
- **`obj/` "empty scene" investigation — mostly confirmed-correct-as-is, conclusions in docs/ARCHITECTURE.md's "Known deferred work."** Of 1068 mounted `obj/` FLVER0 files, 62 have no mesh: 57 are genuine dummy-only attach/anchor markers, 5 are bare allocated-but-never-built stubs. Neither is fixable by a smarter resolution rule — there's no mesh anywhere to find.
- **FLVER0 vertex-layout crash — two crash sites, one root cause, closed and confirmed corpus-wide (zero errors across ~3411 files); surviving fallback logic documented in docs/ARCHITECTURE.md.** Some meshes' `BufferLayout` genuinely has no Normal/UV/VertexColor member at all — not a parser bug. Needed two fixes: the importer's own vertex loop (guard on `.Count > 0`, fall back to sane defaults) and one level deeper in `SoulsFormatsNEXT` itself (`Mesh.Triangulate`'s `doCheckFlip` crashed on the same meshes — gated at the call site rather than passing `false` unconditionally, which would have silently broken winding-correctness on strip meshes that legitimately need the check).
- **Lightmap system added, then parts 2-3 fixed two shader-compile bugs and a `WorldEnvironment`/sky gap — all now fully described as current behavior in docs/ARCHITECTURE.md's Lightmaps section.** The lightmap is a genuine per-mesh UV atlas bake, confirmed via manual decode before committing to any shader architecture. `blend_mix`/`blend_add` being compile-time `render_mode` keywords (not a per-material property) is why there are three thin shader variants instead of one runtime-switched shader; `Custom0`'s component format has no explicit-array-format parameter in 4.7's `SurfaceTool` C# binding, worked around via `ImporterMesh.AddSurface`'s `flags` parameter. Part 2's two real shader-compile bugs (assigning built-ins from a helper function; reading `CUSTOM0` outside `vertex()`) are both invisible under `--headless` and only caught by a real Xvfb+opengl3 driver check — now standing restrictions documented in ARCHITECTURE.md. Part 3: darkness/contrast root-caused to a missing sky in the scene's `WorldEnvironment`, not a shader/color-space bug — ruled out sRGB/gamma mismatch directly via pixel-readback test first. Two still-generally-useful process lessons from this stretch: a background `godot-mono --import` launched via a trailing shell `&` can report "complete" almost instantly while the real process keeps running detached — verify via `ps aux`, don't trust a quick tool-call return; and reflection into a private method (`ResolveTexture`/`DecodeTexture`) is a legitimate way to test the *exact* pipeline in a throwaway probe rather than reimplementing it by hand.
- **Lightmap system, part 4 — three shader-only contrast/artifact fixes attempted, all reverted; real architecture correctly identified (hemisphere-ambient floor + lightmap-scaled env term, not a plain multiply) but blocked at the time on unparseable per-map color data.** Real DeS MTD data confirmed these materials are tagged `g_LightingType = HemEnvDifSpc`. Community ground truth (`AltimorTASDK/dsr-shader-mods`, decompiled Dark Souls Remastered HLSL — same FRPG engine lineage, not byte-exact to DeS's 2009 shader but the only concrete source found) showed the real shader never multiplies the lightmap against the base diffuse/ambient result — it only scales a secondary env/IBL term, while an always-on two-color hemisphere-ambient term (blended by vertex-normal Y) is added on top, untouched by the lightmap. Three attempts, all reverted: (1) a flat neutral floor fixed the crush/blowout but looked "dull," diluting the real warm-toned baked color; (2) a hue-preserving reciprocal boost blew up DXT block-compression noise into visible purple/green/magenta patches via division by near-zero luminance; (3) an additive floor tied to diffuse color (matching the real shader's actual structure, no division anywhere) still showed color patches, which was itself a finding — it pointed away from the compositing formula and toward the raw lightmap texture decode as the artifact's real source. Reverted to the original plain multiply; real fix stayed blocked on per-map hemisphere-ambient color data no format in this project could parse yet.
- **Lightmap system, part 5 — probed the raw lightmap decode directly; found real evidence the block-noise artifact is a block-read-alignment problem, not organic DXT noise. Later disproven as PS3-swizzle-related (part 9) and ultimately closed as an original-game imperfection (part 10), left here for the process record.** Decoded every `*_lit_*` texture in `mounted/map/m01` and visually confirmed block-granular purple/magenta/green speckle noise aligned to 4×4 block boundaries. Ruled out ordinary DXT1 quantization error by manually decoding the worst offending block per the BC1 spec — the achievable palette was `[0,8]` per channel, but Pfim's actual output was `(19,19,19)`+, values that block's own bytes cannot produce under any interpolation. Leading suspect (not confirmed): PS3 block-level swizzling for compressed formats, never undone by `Headerizer.cs`'s `ReadPS3Images` (which only swizzles a short list of *uncompressed* formats). Deliberately not chased further per user decision — no decompiler tooling available to inspect Pfim's real read order, and reverse-engineering the true PS3 block-swizzle pattern would mean changes to the `SoulsFormatsNEXT` submodule itself.
- **Lightmap/drawparam system, part 5 — an outside contact with DeS reverse-engineering knowledge, asked cold, confirmed drawparam colors and vertex color both matter for DeS shading and are applied via per-part `LightID`/`FogID`-shaped MSB fields referencing `.param` rows.** No literal `DrawParam` format exists in `SoulsFormatsNEXT`; the graphics-config formats that do exist (`GPARAM`/`BTL`/`BTAB`/`BTPB`) are all confirmed DS2-and-later via their own doc comments, and zero `.btl`/`.gparam` files exist in the mounted extraction — DeS predates that format lineage entirely. `MSBD.PartsParam.LightID`/`FogID` (both marked `Unknown` upstream) already existed, parseable, unused — matching the contact's hazy recall. The actual color/ambient values live in generic `.param` rows, not a dedicated format — `PARAM`/`PARAMDEF` support already exists generically, but the specific target paramdef wasn't identified yet at this point. Recommended and later-taken next step: a small throwaway investigation to confirm what `LightID`/`FogID` actually resolve to before committing to real MSB-parsing plumbing — same "verify the premise before building on it" lesson as the DSR-shader-approximation caveat in part 4.
- **Lightmap/drawparam system, parts 6-8 — `LIGHT_BANK`/`FOG_BANK` confirmed against real data and wired into the shader; now fully described as current behavior in docs/ARCHITECTURE.md's Lightmaps section.** Row IDs/names line up 1:1 between `LIGHT_BANK`/`FOG_BANK` (confirmed, not inferred). A distinct, worse regression on Boletaria (hard-edged dark patches, no gradient) surfaced only via real in-editor testing — `terrain_blend.gdshader` hadn't gotten the hemisphere/env formula yet; porting it there fixed it, user-confirmed.
- **Lightmap/drawparam system, part 9 — part 5's PS3 block-swizzle suspect tested directly and disproven (2026-07-23).** An external comparison against Soulstruct's format-generic PS3 deswizzle (including DXT1/BC1, unlike this project's uncompressed-only gate) looked like strong corroboration. Applying it produced the *opposite* result: a before/after dump on a real lightmap texture showed the pre-fix texture coherent and the post-fix (deswizzled) one block-scrambled — deswizzling *causes* the corruption, doesn't fix it. Root cause of the wrong lead: `Headerizer.Headerize`'s own doc comment already said DeS/DS1 don't use PS3 swizzling for most formats — sitting in the exact function touched, overlooked before making the change. Reverted, submodule confirmed clean. Process lesson: read every doc comment/guard on the exact function being touched before trusting an external comparison, however well-evidenced.
- **Lightmap/drawparam system, part 10 — the purple/green/magenta block-noise artifact (parts 4/5/9) closed as not-a-bug (2026-07-26).** The user played real, unmodified Demon's Souls and found the same faint green/magenta artifacts on the same Nexus geometry, at the same subtlety now visible after the hemisphere/env fix — an original-game characteristic, not an importer decode bug. No longer listed as open work.
- **Nexus VFX gaps investigated (2026-07-26) — two real, separate root causes for map-piece "glow"/"animation" materials looking flatter than the real game, both scoped as future work.** `m3051b0`'s sun-ray quads carry real, authored `g_TexScroll` UV-scroll data (`A05_vollight[Dn]_Add.mtd` family, confirmed present under multiple areas) currently only wired for water materials — `BuildStandardMaterial` has no scroll path at all. Separately, `m3030b0`'s runes and `m9501b0`'s sky dome carry real `g_DiffuseMapColorPower` values meant to make them glow, but the tint math is a no-op whenever the base tint is pure white (true for the large majority of materials, including both of these). An `Emission`-based fix was tried and reverted: real rendering showed `Emission`, unlike `Albedo`, renders badly wrong (desaturates toward flat white) with no `WorldEnvironment`/`CameraAttributes` anywhere to calibrate exposure against — reproduced with a synthetic texture, zero FLVER involvement, ruling out a decode bug. Genuinely blocked on the same missing-environment gap as water/lightmap tuning, not a smaller fixable issue. (Later superseded for the sky-dome case specifically by the `g_LightingType=0` fix, see the 2026-08-18 entry below — the real mechanism turned out to be "skip ambient entirely," not "boost emission.")
- **MSB-parsing next-step scouting (2026-07-26) — confirmed the `MSB1`-can't-read-DeS blocker was stale, and confirmed `LightID`/`FogID` genuinely vary per-part rather than being near-constant per map.** DeS uses `MSBD` (its own reader), not `MSB1` — the `Treasure.ReadTypeData` assert bug lives only in DS1-specific `MSB1` code this project never calls; corrected in `docs/PLAN.md`/`docs/ARCHITECTURE.md`. A temp debug dump against four real maps confirmed `MSBD.Read()` completes cleanly end to end and that `LightID` varies real and wide within a single populated map (real per-category numbers now live in `docs/PLAN.md`'s Phase 1 item 4, not repeated here) — investigative only, no code kept from this pass.
- **The old `EditorSceneFormatImporter` pipeline (deleted 2026-07-24) absorbed several rounds of real stability work before being replaced outright — compressed here since none of the mechanism still exists.** In order: a bulk-reimport-only `o0103.flver` hang (unbounded per-instance texture cache growth); a batching mitigation that needed a second fix after wiping its own restart-recovery on the first test; a texture-cache self-eviction fix (`MaybeEvictDecodedTextures`, still present today, this part outlived the importer it was written for); and a concurrency gate that, after confirming directly against Godot's own engine source that no lever exists to control reimport concurrency at all, still didn't fix the underlying crash cycle. That last dead end — Godot gives a plugin no control over its own reimport concurrency — motivated the architecture pivot below, not another patch attempt.
- **The `res://`-bypass pivot — prototyped from a peer developer's real reference implementation (thanks Nox, see `.local-notes.md`), then actually implemented, and is now the shipped architecture (2026-07-24), user-confirmed working, fast, and the best state this plugin has ever been in.** Four real bugs surfaced during implementation, each found by testing against real data: (1) an unrelated, pre-existing hard engine crash on `m2011b1.flver` (confirmed pre-existing by reproducing it on the un-split code too) — not root-caused, flagged as a known open issue, moot for the reimport-queue path specifically since that path no longer exists; (2) a prototype custom `Tree`-based file browser (`mounted_browser_dock.gd`) whose expand arrows never worked in the live editor despite verified-correct handler logic in isolation — abandoned in favor of the already-proven `EditorFileDialog` pattern rather than continuing to debug blind, deleted outright; (3) the one that actually mattered: extraction still triggered the exact crash-prone reimport cycle the pivot existed to avoid, via a leftover `EditorInterface.get_resource_filesystem().scan()` call and the old importer still being registered — fixed by removing both, `FlverSceneImporter.cs` deleted; (4) loaded models had correct structure but were completely invisible — `ImporterMesh`/`ImporterMeshInstance3D` are import-pipeline-only placeholder types with no rendering of their own, and `FlverLoader` never goes through that pipeline. Fixed via `ImporterMesh.GetMesh()` → real `ArrayMesh`/`MeshInstance3D`. **Process lesson**: two of the four bugs were found only because the user actually exercised the feature end-to-end in the real editor — neither `dotnet build` nor a headless structural smoke test caught them, the same category `--headless`'s dummy rendering driver already can't catch for shader errors.
- **Armor/character specular flatness — root-caused and fixed (2026-07-24), now fully described as current behavior in docs/ARCHITECTURE.md's roughness section.** `Roughness` sat at `StandardMaterial3D`'s own default (`1.0`, fully rough) since only `Metallic` was ever set from `g_Specular` — silently true for every non-lightmapped chr/parts material, not armor-specific. Verified against real `c9990` data that the fix reads genuine per-material values, not one hardcoded number.
- **Destructible-prop debris cluster (`o6511`-`o6602`) — investigated, root-caused as genuinely out of importer scope for now.** Verified directly against raw, unmounted PS3 files: the shipped game data for these objects really does contain 0 triangles. Found `map/breakobj/*.breakobj` (20 files, magic `OBJB`, completely undocumented) as the likely real geometry source — working theory is a Havok-destruction system composing debris at runtime from a small shared chunk library, which would mean no per-file resolution rule could ever recover it. See docs/ARCHITECTURE.md's "Known deferred work."
- **Lightmap/drawparam system, part 10 — scouted (not implemented) whether DeS's unused/underused armor reflection system is real; confirmed as a genuine cubemap system, not a matcap-style trick like water's.** Scouting only, per the user's own framing — no code changed. **Conclusion: real, extractable, structurally-legible data — a genuine reflective-cubemap upgrade for armor materials is possible, not just rumored.** Findings folded into docs/ARCHITECTURE.md's "Character/parts specular accuracy" bullet, which also lists the concrete blockers (param/paramdef extraction, unconfirmed per-map cubemap resolution, the same MSB per-part resolution work `LIGHT_BANK` needed, and no cubemap slot on `StandardMaterial3D`).
- **MSB map-piece placement — first slice implemented, 2026-07-24, fully described as current behavior in docs/ARCHITECTURE.md's "MSB map placement" section.** New `MsbLoader.cs` mirrors the `FlverModelBuilder`/`FlverLoader` split (pure parsing, no scene nodes). Real finding, corpus-checked before assuming the transform math mattered: most `MapPiece` parts carry an identity transform (geometry pre-baked into the FLVER's own world-space data), but a real minority are genuinely reused at multiple instances per block and need a real per-instance transform — varies enormously by map (Boletaria 1/61, `m08_00_00_00` 84/85). Even before rotation correctness was confirmed, the membership filter alone was real working value: it distinguishes placed `.flver`s from unused leftovers for the first time. Position/X-negation matched `FlverModelBuilder`'s own convention and was structurally verified; rotation was a starting hypothesis at the time, later spot-checked (see "MSB map placement" in ARCHITECTURE.md). Scoped to `MapPieces` only — `Objects`, `Enemy`/`Player`/`Collision`/`Navmesh`, and drawparam wiring all deliberately deferred to later slices.
- **Rigid mesh-to-node binding — the real cause of "decorations sitting at the world origin," root-caused and fixed via real user testing of the MSB slice (2026-07-24), now fully described in docs/ARCHITECTURE.md's coordinate-conventions section.** Found only because the user tested "Load Map..." in the real editor — a category of gap headless checks structurally can't cover. A re-test after the first fix caught a real gap in it (a mesh can mix vertices bound to *different* palette entries, not just one per mesh). Process note worth keeping: a headless verification script hung indefinitely for reasons never root-caused — add progress prints and a hard timeout by default rather than trusting a long wait as progress.
- **`m2304b0` window one-sidedness — root-caused and fixed (2026-07-25), fully described in docs/ARCHITECTURE.md.** `FLVER0.Mesh.CullBackfaces` was correctly parsed off disk but never once read anywhere in `FlverModelBuilder.cs`. Corpus-scanned before fixing: 713/3411 files have at least one affected mesh; separately found 39 real files where meshes with different `CullBackfaces` share one `MaterialIndex` (a second latent bug — `GetOrBuildMaterial`'s cache key gained `CullBackfaces` alongside `materialIndex`). Deliberately scoped to `StandardMaterial3D` only, matching where the reported bug actually lived — `ShaderMaterial`'s cull mode is a compile-time keyword, so the 338 lightmap-path double-sided meshes are still open (see ARCHITECTURE.md's "Known deferred work"). Process note: this exact suspect had been named once already (the rigid-node entry above) without being followed through — naming a plausible cause isn't the same as ruling it out.
- **Post-`CullBackfaces` sweep (2026-07-25) — systematically checked every unread FLVER0/MTD field against what `FlverModelBuilder.cs` actually reads; three real fixes shipped, three more found and deferred.** Shipped: `g_SpecularPower`→roughness extended from chr/parts to the lightmap/blend `ShaderMaterial` family (63% of all materials in the game, previously untouched — refactored into a shared `ResolveMtdShading()`); `g_DiffuseMapColor`/`g_DiffuseMapColorPower`, a real per-material tint read by nothing before this; `g_BumpMapSmoose` on water materials. Found and deferred (write-ups in ARCHITECTURE.md's "Known deferred work"): `g_BlendMode` (doesn't correlate cleanly with the existing filename heuristic), `g_LightingType` (applied unconditionally at the time, not yet gated on), and a real `g_IsGhost` material family needing its own shader. Ruled out: `FLVER.Node.Flags` (always `0` across 51,923 nodes — not a real signal in DeS data), `Tangent`/`Bitangent` (real but low-impact vs. Godot's auto-generation).
- **The lightmap/blend roughness fix immediately regressed most map surfaces into a washed-out, glassy sheen (2026-07-25) — root-caused, "fixed," then found NOT actually fixed, then reverted outright the same day.** Root cause diagnosed as a pre-existing `metallic = spec` line, invisible before because `ROUGHNESS` used to default to Godot's fully-rough `1.0` — with zero `WorldEnvironment`/reflection probes anywhere, a nonzero `METALLIC` at real (lower) roughness reads as a flat misplaced sheen with nothing real to reflect. First fix (`metallic = 0.0`) did **not** actually resolve it on user re-test — the diagnosis was plausible and internally consistent but wrong, or not the whole story. Rather than keep guessing same-session, the whole roughness/metallic change on this material family was reverted outright (not commented out — matches this project's standing convention for abandoned attempts, see the PS3-deswizzle entry above), keeping only the unrelated diffuse-tint read and the original chr/parts roughness fix. Needs real `WorldEnvironment`/lighting groundwork and probably `g_LightingType` gating before it's worth re-attempting — see docs/ARCHITECTURE.md's "Known deferred work." Process note: extending a fix validated on one narrow case to a whole material family needs checking every simplification already baked into the destination shader, not just the one property being added.
- **Lightmap/drawparam system, part 11 — real RPCS3-captured DeS RSX shader bytecode confirms the hemisphere/env formula shape for the first time; the `env_intensity` overexposure bug identified as blocked on missing prerequisite data, not a formula guess (2026-08-17).** Captured two play sessions (Boletaria, Nexus) via RPCS3's shader-program logging. Interpreter-fallback shaders (a generic template with zero per-material info, ~30% of dumped files) distinguished from real ones by grepping for `RSX_FP_OPCODE_NOP`/`RSX_VP_OPCODE` markers and discarded. **Confirmed, high confidence**: the hemisphere two-color ambient blend matches `hemisphere_ambient.gdshaderinc`'s formula exactly, found independently across 7+ real shaders. **Confirmed, and a correction to this doc's prior DSR-based framing**: real bytecode sums the env/reflection term into the *same* accumulator as the hemisphere blend before the diffuse multiply — matching this project's shipped formula exactly; no shader code changed, only the doc's framing. **New lead, not yet actionable**: the real env term is gated by `min(shadow_attenuation, lightmap_sample)` before scaling, not an unclamped multiply (`FragmentProgram298.glsl`) — this project has neither a dynamic shadow-map system nor a confirmed per-map env-reflection texture to gate with, so this explains why a real fix needs prerequisite data, not a drop-in formula. Also confirmed: `m0000B0` (the cited overexposure example) is Old One/ending-area geometry, not present in the regular explorable Nexus hub — corrected in `ARCHITECTURE.md`/`PLAN.md`.
- **Lightmap/drawparam system, part 12 — a broader research pass over the same two captures, which surfaced and corrected a real token-search methodology flaw in its own first pass (2026-08-17).** A first pass (delegated, spot-checked by hand after its conclusions looked suspicious) reported zero cubemap sampling and zero alpha-cutout across 326 real shaders, based on grepping high-level GLSL names (`TEXCUBE`/`reflect`/`discard`). **Both were false negatives**: RPCS3's RSX decompiler never emits those names — cubemap sampling goes through the same generic `TEX3D` macro as regular textures (disambiguated only by the texture unit's declared type; confirmed 207/1086 real files genuinely invoke a declared `samplerCube`), and alpha-cutout goes through a `_kill()` macro (470/1086 real files). Standing lesson: grep the decompiler's own macros (`TEX2D`/`TEX3D`/`TEX2D_SHADOWPROJ`/`_kill`/`_fetch_constant`), never hand-written-shader function names. Corrected finding: real cubemap sampling is confirmed and load-bearing (strengthens part 11), though whether it belongs to chr/parts `EnvSpc` or map `LIGHT_BANK` `envSpc` still can't be told apart from bytecode alone. Fog confirmed a genuine, cleanly-closed capability gap (every real shader defines an unused `fetch_fog_value()`, never called in either session — both played in fog-disabled interior areas). Ghost alpha-cutout and water remained genuinely open at this point (water resolved in part 13 below).
- **Lightmap/drawparam system, part 13 — a full RSX-capture methodology pass identifies water by name, strengthens the chr/parts `EnvSpc` cubemap hypothesis, and closes the "is shader calibration data hiding in an unparsed file" question for good (2026-08-17).** Live-pausing RPCS3 to catch a specific draw (e.g. "the water surface") doesn't work — confirmed by repetition, both attempts landed on the same post-process composite pass, not scene geometry; not worth retrying as a targeting strategy. Static "puzzle piece" identification of water converged from three independent signals (real MTD-parsed texture dimensions setting the search shape; a distinctive chained-sample pattern in `FragmentProgram35.glsl`; sparse vertex-attribute usage in its paired vertex shaders) — meaningfully stronger than any one heuristic, still short of ground truth. **Ground truth arrived from an entirely different direction**: `shader/ds_flver.shaderbnd` (a real `BND3` archive, never in `AssetExtractor.KnownCategories`) contains 1349 files with the original developer's own filenames intact — `DS_Water_Env.fpo`/`DS_Water_Mask.fpo`/`DS_Water_Reflect.fpo` confirm water by name, and the broader naming convention confirms "HemEnv" is FromSoft's own term, not a DSR-proxy inference. **The catch, checked directly**: these `.fpo`/`.vpo` binaries carry real RSX instructions but no embedded parameter-name table (`strings` scan empty, even in this Debug build) — closes the "keep looking for a file" question for good; a follow-up survey of every other top-level game-root category found no such table anywhere else either. Real per-material numeric calibration now has exactly two remaining paths: disassembling the raw RSX instruction buffer, or the project's existing visual-reference-screenshot fallback. Side effect, unrelated to shader calibration: a large previously-unknown category of readable, unextracted game data surfaced (`script/` — 373 files, real plain-text `.lua` + `.esd` state machines; `menu`/`sfx`/`remo`/`msg`) — `SoulsFormatsNEXT` already has working readers for all of it, none wired into `AssetExtractor.KnownCategories` or scoped into `docs/PLAN.md` yet.

- **Havok/animation ecosystem research (2026-08-18) — SoulsFormatsNEXT synced from upstream, then three external codebases surveyed for what could close the still-open skeleton/animation gap.** Every claim below was independently verified by fetching the actual source (raw file content, not READMEs/marketing claims) before being trusted - this session's own prior lesson (Soulstruct's PS3 deswizzle lead, part 9 above) is exactly "external corroboration is a lead to test, not a fact to trust."
  - **SoulsFormatsNEXT upstream sync.** Merged 3 new upstream commits (`ee1dd61`/`29b6707`/`8db9798`: a `METAPARAM` reader, FLVER2-without-vertex-data support) into our fork's one local patch (`d1de279`, the FLVER0 UV-scale fix + net8.0 retarget) - zero file overlap, confirmed clean via a dry-run `git merge --no-commit`/`--abort` before the user did the real merge/push themselves.
  - **`NVM.cs` (DeS navmesh) - a free win, already in our submodule, completely unused.** `SoulsFormatsNEXT/SoulsFormats/Formats/NVM.cs` (406 lines) already reads DeS navmesh end-to-end (vertices/triangles/box hierarchy/per-triangle flags, DeS/DS1 byte-order auto-detect) - grepped `addons/archstone/`, zero references anywhere. Not a SoulsFormatsNEXT patch at all, a wiring task on our side following the same `BuildPlacements()` pattern `MapPieces`/`Objects` already use.
  - **The `old-kata-2018-dec-13` branch - real, DeS-scoped HKX collision, sitting unmerged in our own upstream's history.** `soulsmods/SoulsFormatsNEXT` (our submodule's `upstream` remote, reachable via `git branch --all --contains`) has a never-merged 2018 branch (commits `29cd70b`/`73fd675`, author "Katalash") adding `Formats/HKX/HKX.cs` (1139 lines) + `Collision.cs` (563 lines). Confirmed by reading the actual code: `HKX.cs`'s magic number (`0x57E0E057`) identifies it as a Havok **packfile** reader (the older/PS3-era container format), with an explicit `HKXVariation.HKSDeS` case keyed off the version byte (`0x05`) - Demon's Souls is a first-class case here, not bolted on. `Collision.cs`'s `HKPStorageExtendedMeshShapeMeshSubpartStorage` is explicitly commented "Used in DeS/DS1/DS2 to store collision mesh data" and exposes raw `Vertices` (`Vector4[]`)/`Indices16` (`ushort[]`) - functional (the author's own comment: "By no means complete but currently quickly extracts most meshes") but rough: no convenience mesh-extraction API (the object graph has to be walked by hand to find the right instance), `Write()` unimplemented, and untested against our actual corpus. **No animation/skeleton parsing exists in either file**, despite `HKX.cs`'s class doc-comment mentioning animations generically - that line just describes what Havok files can generically hold. Same lineage as `github.com/Meowmaritus/MVDX_Classic`'s `HKX/` folder (the user's original tip that started this investigation) - confirmed identical file set via `WebFetch`.
  - **`SoulsAssetPipeline`'s `Havoc/` folder - better architecture, but hard-excludes Demon's Souls.** `github.com/Meowmaritus/SoulsAssetPipeline` (GPL-3.0, confirmed via its `LICENSE`), root-level `Havoc/`: a genuine *generic* reflection-based Havok deserializer - reads Havok's own embedded type metadata from the tagfile and deserializes arbitrary classes (skeletons, animations, collision, anything) without a hand-written C# class per Havok type (`Havoc/Reflection/`, `Havoc/IO/Tagfile/`). But `Havoc/HkSdkVersion.cs`'s `SupportedSdkVersions` array - independently re-verified via direct raw-file fetch, not just trusted from the first pass - is `{V20150100, V20160100, V20160200, V20180100, V20190100}`, 2015-and-later only; DeS's Havok 5.5.0 generation isn't remotely in range (older even than the one excluded fallback, `V20120200`), and the binary tagfile reader actively rejects anything outside the list. `DSAnimStudio` (same author, GPL-3.0, animation-focused) has no Havok code of its own - it pulls `SoulsAssetPipeline` in as a submodule, so it inherits the same version gate. Net: a good reference for the *pattern* (generic reflection deserializer beats hand-coding one class per type), not usable for DeS as-is.
  - **`soulstruct-havok` - the real find: genuine, from-scratch, DeS-specific Havok animation/skeleton parsing, in Python, GPL-3.0-or-later.** `github.com/Grimrukh/soulstruct-havok` - a separate package from `soulstruct-blender`, and actually public despite the latter's stale "not public yet" README claim (confirmed directly). `src/soulstruct/havok/fromsoft/demonssouls/core.py` (independently re-verified via raw-file fetch): DeS-specific classes explicitly labeled **"Havok 5.5.0"**; `SkeletonHKX` (`hkaSkeleton`/`hkaBone`), `AnimationHKX` (`hkaAnimationContainer`/`hkaSkeletalAnimation`), and `CollisionHKX` (`hkpPhysicsData`/`hkpPhysicsSystem`) are all real, working classes. Critically, reading goes through `havok/packfile/` specifically, not `havok/tagfile/` - the same older/PS3-era container format `old-kata`'s `HKX.cs` targets, i.e. this library actually branches its parser by Havok generation, which is exactly what `SoulsAssetPipeline`'s tagfile-only reader fails to do for DeS. **One real implementation detail worth knowing before scoping any port:** `core.py` itself flags DeS animations as **wavelet-compressed**, calling it "an annoying old format to deal with" - a different, reportedly harder compression scheme than later titles' spline-compression, so this is not a "same shape, older tag" port. The package bundles Windows EXEs (`CompressAnim.exe`, `mopper.exe`, etc.) but these are write/export-path only (re-compressing animations, generating collision MOPP trees) - reading needs none of them. Collision reading goes through the real `hkpPhysicsData` object graph (more complete than `old-kata`'s one hand-picked mesh-shape class), but whether it exposes per-triangle terrain-type/material flags wasn't confirmed - would need a follow-up read into `havok/types/`.
  - **Net effect on the roadmap.** Animation/skeleton import is no longer "nothing scoped, from-scratch reverse-engineering" - a concrete two-source approach exists: `old-kata`'s C# packfile-parser as the base (it already targets the right container format for DeS), extended with hand-coded `hkaSkeleton`/`hkaBone`/`hkaAnimationContainer`/`hkaSkeletalAnimation` classes and DeS's wavelet-decompression algorithm, using `soulstruct-havok`'s Python purely as the field-layout/algorithm reference since none of it ports directly to C#. **User decision (2026-08-18): still deliberately deferred until after the graphical side of the importer is largely done** - even with parsing solved, this project has no skeleton/animation runtime at all yet, the same "no game logic to plug into" reasoning already applied to `Enemy`/`Player` MSB placement (see docs/PLAN.md's Phase 1 item 4). See `docs/PLAN.md`'s Phase 1 item 2 and `docs/ARCHITECTURE.md`'s "Known deferred work" for the current framing.

- **A first `WorldEnvironment` was built, tried in-editor, and reverted the same session (2026-08-18) - not because the wiring was wrong, but because tonemap/exposure/ambient have no real DeS data behind them and the result read visibly too bright.** `DrawParamReader.GetDefaultLightBankRow()` (a one-line wrapper around the existing `LoadParam("default", "lightbank")?[0]` fallback path `GetRow` already used internally), `FlverLoader.BuildDefaultWorldEnvironment()` (built a `Godot.Environment` with `TonemapMode=Filmic`, `TonemapExposure=1.0`, `AmbientLightSource=Color` sourced from that default row's `_d` (ground) fields), and `archstone.gd`'s `_on_maps_selected` hook (`_ensure_world_environment()`/`_find_world_environment()`: a scene-tree walk that adds exactly one `WorldEnvironment` under the edited scene's root, skipping if one already exists, so loading several maps into one scene doesn't stack duplicates) - all worked correctly and compiled/ran clean. User-tested in-editor: `Filmic` was a real improvement over the Godot default, but the overall result was still "too bright," and testing it surfaced (separately from the WorldEnvironment work itself) that a real map lightmap/shading-accuracy gap still exists, suspected to be related to the still-open `env_intensity` overexposure/shader-env questions already tracked in `docs/ARCHITECTURE.md`'s "Known deferred work" - not something to chase yet. **Reverted via plain `git restore` on the three touched files** (no prior uncommitted work in any of them, so a clean revert), keeping only the unrelated `g_LightingType` sky-fix that shipped the same session (see the entry below). **Worth reusing, not re-deriving, next time this is attempted for real:** the three-piece wiring above (default-row helper -> `Environment`-builder method -> scene-tree-walk dedup hook in the map-load path) is the whole mechanism; only the actual tonemap/exposure/ambient-energy numbers need real data (or better calibration) before this is worth re-adding. Needs real reference-screenshot calibration (the same standing gap already blocking water/lightmap-roughness tuning) beyond just fog data, contra this session's initial assumption that fog was the only piece still missing.

- **`g_LightingType=0` identified and fixed (2026-08-18) - explains a real, user-reported "sky reads washed-out/scene-ambient-affected" bug, and closes three previously-separate deferred-work threads at once.** A full corpus scan of all 612 mounted `.mtd` files (a throwaway `dotnet run` console harness referencing `SoulsFormatsNEXT` directly, not a headless Godot script - `MTD.Read()` needs no Godot API at all) found a clean three-way `g_LightingType` split: `1`=Phong (chr/parts metal/leather family, already the roughness fix's basis), `3`=HemEnv (lightmap/blend family), and `0` - previously unconfirmed, guessed at in `docs/ARCHITECTURE.md` as "plain diffuse-only Lambert" - which turned out to be every sky-dome MTD (`M_Sky[Dn]`/`M_Sky_dark[Dn]`/`M_Sky_light[Dn]`/every `a03_sky*` variant), `Ps_Wander_Ghost` (the already-flagged, still-unhandled ghost/dissolve family), and every additive-VFX material already investigated separately (`m99_vollight*`, `a04_light_shaft`, `a03_cloud`, `a05_water02_Add`, `a07_yuki` snow). All share one real shader family (`DS_Phn_ColDif.spx`), confirmed via direct MTD reads, not inference. Fix: `FlverModelBuilder.ResolveMtdShading()` now also reads `g_LightingType`; `BuildStandardMaterial` sets `ShadingMode=Unshaded` when it's `0` - skips Godot's ambient/dynamic lighting entirely rather than trying to boost `Emission` (the mechanism the existing "Emission-based VFX glow" revert, cited elsewhere in this file, had already ruled out - this needed no `WorldEnvironment`/exposure calibration at all, unlike that attempt). Verified via two headless checks (not just a clean build): `m9501b0.flver` (the real Nexus sky dome, `M_Sky_light[Dn].mtd`) now resolves `g_LightingType=0` and ends up `ShadingMode=Unshaded` (confirmed the enum value itself first - `SHADING_MODE_UNSHADED` is `0` in Godot 4, not `SHADING_MODE_PER_PIXEL`, an easy initial misread); `c9990.flver` (the chr metal-armor file the roughness fix was validated against) is untouched, still `PerPixel` with unchanged roughness values - no regression on the normal-lit path. **User-confirmed in-editor (2026-08-18): map skies now read colored/bright and are no longer visibly affected by ambient/ground color, matching the diagnosis.** The `_Add`-tagged members of this same family (light shafts/vollight dust/cloud) still don't animate (`g_TexScroll_0`/`_1`/`_2` are real, authored, unread by `BuildStandardMaterial`) - a separate, now-unblocked small follow-up, not done this pass.

- **A third-party investigation brief (`soulbrandt_lighting_investigation_brief.md`, user-supplied 2026-08-18) led to a real, independently-verified new find - `ToneMapBank`/`ToneCorrectBank` - but wiring it into `WorldEnvironment` caused a genuine regression (pure-black areas), not an improvement, revealing a real gap in understanding, not just a calibration problem.** The brief itself was mostly a restatement of this project's own prior findings (it appears to have been built substantially from `LIGHTING_REFERENCE.md` and this file), but flagged `ToneMapBank`/`ToneCorrectBank` as something from "external DeS modding research" worth checking. Verified directly against real mounted data rather than trusted: `mounted/paramdef/ToneMapBank.paramdef` (`bloomBegin`/`bloomMul`/`grayKeyValue`/`minAdaptedLum`/`maxAdapredLum`/`adaptSpeed` - a real Reinhard-style eye-adaptation block) and `ToneCorrectBank.paramdef` (`brightnessR/G/B`, `contrastR/G/B`, `saturation`, `hue`) both exist, both have real per-map `.param` files (`m01`-`m08`/`m99`/`default`), and - critically - `MSBD.Part.ToneMapID`/`ToneCorrectID` (already parsed by `SoulsFormatsNEXT`, marked "Unknown", never read by this project) index into them exactly the way `LightID`/`FogID` already do. Confirmed genuinely per-part-varying, not padding: Nexus `MapPieces` span `(ToneMapID, ToneCorrectID)` in `{(0,0), (0,1), (1,1)}`, and `m01_tonemapbank.param`'s rows carry real, different `bloomBegin`/`bloomMul`/`grayKeyValue`/adaptation values per row - `default_tonemapbank.param` by contrast is flat and matches the paramdef's own defaults everywhere, i.e. a true no-op fallback.
  - **Reading wired first, data-only** (`DrawParamReader.GetToneMapBankRow`/`GetToneCorrectBankRow`, `MsbPlacement.ToneMapID`/`ToneCorrectID` via `MsbLoader.BuildPlacements`) - kept, see `FlverModelBuilder.cs`/`MsbLoader.cs`/`DrawParamReader.cs` for the current state.
  - **A consumer was then built and user-tested, and reverted the same session - not because the data was fake, but because applying it made things visibly worse.** `FlverLoader.BuildWorldEnvironmentForMap()` picked each map's most-common `LightID`/`ToneMapID`/`ToneCorrectID` (`ModeOrDefault`, since Godot's `WorldEnvironment` is still one node per scene) and mapped `grayKeyValue` to `TonemapExposure` (ratio against the `0.18` standard-photographic baseline `default_tonemapbank.param` itself uses), the map's own `LightID` row's `_d` (ground) fields to `AmbientLightColor`/`Energy` (replacing the earlier flat default-row guess), and `ToneCorrectBank`'s per-channel `brightness`/`contrast` (averaged to Godot's single-scalar `Environment.AdjustmentBrightness`/`Contrast`, `hue` dropped - no Godot `Adjustment` equivalent exists) to `Environment.AdjustmentEnabled`/`Brightness`/`Contrast`/`Saturation`. Auto-exposure (`minAdaptedLum`/`maxAdapredLum`/`adaptSpeed` -> Godot's `CameraAttributes`) and bloom (`bloomBegin`/`bloomMul` -> Godot's Glow) were deliberately left unwired - no confident unit conversion, flagged in-code rather than guessed. Verified headlessly with real (non-default) numbers before being shown to the user: Nexus resolved to `tonemap_exposure=1.0` (a real coincidence - the mode-resolved row's `grayKeyValue` happened to be exactly `0.18`), `ambient_light_energy=0.05`, `adjustment_brightness≈0.83`, `contrast≈1.1`, `saturation≈0.9`.
  - **User-tested in-editor (2026-08-18): a real regression, not an improvement - most maps went dark, with some areas going pure black, and neither re-tweaking the lighting data nor the tonemap settings could poke a hole back through it.** User's own framing: "There is definitely a real gap here in what DeS does vs what we both have and understand." Tonemap changes alone were confirmed fine (didn't meaningfully move the needle either direction) - the `AdjustmentBrightness`/`Contrast`/`Saturation` path is the prime suspect, not tonemap. Live candidates, none confirmed: (1) the per-channel-to-scalar averaging is a real lossy simplification that may not preserve the original per-channel intent; (2) Godot's `Adjustment` pass and this project's already-baked-into-`ALBEDO` custom shader math may be double-applying a darkening/contrast effect the real engine only applies once, at a different pipeline stage; (3) the `ModeOrDefault` row-selection heuristic might be picking a genuinely wrong-for-the-scene row; (4) DeS's own formula for applying `ToneCorrectBank` might not be a simple multiply/lerp the way Godot's `Adjustment` sliders are. **Reverted via `git restore` on `FlverLoader.cs`/`archstone.gd` only** (both had zero other pending changes, so a clean revert) - `DrawParamReader.cs`'s bank-row getters and `MsbLoader.cs`'s `ToneMapID`/`ToneCorrectID` plumbing were deliberately kept, per explicit user direction ("keep the logic for reading the data, but again reverse the change that actually resolves it in the editor"). **Not yet root-caused - a real open question, not just "needs more calibration" like the first `WorldEnvironment` revert above.** Whoever picks this up next should start from candidate (2) (checking for double-application against the custom shader's own `hemi + env` math) before re-guessing at scalar values again. **Resolved 2026-08-27 — see the dig entry below**: it was candidate (2) plus a second effect neither candidate above named (the `StandardMaterial3D` population's ambient collapsing to near-black with no lights in-scene), not candidate (2) alone.

- **Lightmap/drawparam system, part 14 — a full investigation dig (2026-08-27) confirmed the DeS paramdefs and drawparam rows are self-documenting (original dev field descriptions/row names, present in `SoulsFormatsNEXT` all along, never printed), resolving several long-open questions with no reverse-engineering; three findings (`g_BlendMode`, `g_TexScroll_0`, vertex colour) were then implemented and user-confirmed in-editor the same session.** Full findings were written to a scratch `docs/DIG_FINDINGS.md` during the dig, folded into `docs/ARCHITECTURE.md`/`docs/PLAN.md`, then deleted — not kept as a separate file. Headline results, condensed:
  - **The 2026-08-18 `WorldEnvironment`/`ToneMapBank` regression (see the "third-party investigation brief" entry above) is resolved, not just still-suspected.** It was a compound, not a single bug: the real `TONE_CORRECT_BANK` values are far too gentle to produce black (`m01` row 0 — exactly what that attempt resolved to — is brightness≈0.83/contrast≈1.1/saturation≈0.9), and the scene has zero Godot light nodes, so the attempt's `AmbientLightEnergy=0.05` replaced the editor's own preview ambient for the *entire* `StandardMaterial3D` population (88-100% of obj/chr/parts meshes, only 14.2% of map geometry) with near-black, while the 74.4%-of-map lightmap `ShaderMaterial` family (computes its own colour, never reads Godot ambient) was untouched — a mild whole-frame `Adjustment` darkening plus most non-map geometry collapsing to near-black. Explains why neither knob alone could reverse it. **New hard ordering constraint this establishes**: give the `StandardMaterial3D` population a real shader driven by `LIGHT_BANK`'s directional-light terms before ever adding a `WorldEnvironment` again — see docs/ARCHITECTURE.md's "Known deferred work" for the `g_LightingType=HemDirDifSpcx3` confirmation this rests on. Framing check, from user review: this is **not** a case for dynamic Godot lights — DeS itself uses dynamic lighting sparingly (mainly a small player-centred light) and never on map pieces, so nothing here calls for `DirectionalLight3D`/`OmniLight3D` nodes; the fix is a shader that reads `LIGHT_BANK`'s per-situation directional data as uniforms, the same way the lightmap family already does for its two terms.
  - **`LIGHT_BANK` has seven lighting terms, this project reads two — real per-map directional lights (94%/73%/83% of referenced rows use them) are the single biggest unimplemented gap.** `FOG_BANK`'s `degRotZ`/`degRotW` are dummy/fog-master-strength, not rotations. Both confirmed straight from each paramdef's own `Description` field. See docs/ARCHITECTURE.md's Known-deferred-work entry for the full field table.
  - **Per-map environment cubemap resolution, previously unconfirmed, recovered exactly**: `map/<mXX>/<mXX>_9999.tpf`'s `EnvDif_<mXX>_<NNN>`/`EnvSpc_<mXX>_<NNN>` cubemaps, `NNN` = `LIGHT_BANK`'s own `envDif`/`envSpc_0..3` fields — 930/930 referenced rows resolve. `g_EnvSpcSlotNo` (530 MTDs) picks which of the four `envSpc` slots, correlating with `g_SpecularPower` roughly monotonically. Both read-and-resolved, still unwired.
  - **`shader/ds_flver.shaderbnd`'s 1349 dev-named `.fpo`/`.vpo` files decode into a clean, complete lighting-model taxonomy** (`DS_{Phn|Gst}_{feature slots}_{LightingModel}`, arithmetic closes exactly against the observed file count) — confirms `g_LightingType=1` is `HemDir3` (hemisphere + 3 directional lights) independently of `SoulsFormatsNEXT`'s own enum, and that blend/terrain materials are the engine's `Mul` feature slot (explains why their vertex-colour channel carries layer-blend data, not a tint — see the terrain-blend entry in ARCHITECTURE.md).
  - **`shader/ds_filter.shaderbnd`'s 31 files are the complete DeS post-process chain by name** (eye adaptation → bloom → tonemap resolve → DOF/motion-blur/utility). The tonemap resolve (`DS_Fil_HDR.fpo`, 224 bytes) decompiles to a 5-instruction, 2-constant per-channel 1D LUT lookup, not an analytic curve — explains why every `grayKeyValue`→`Environment.TonemapMode` mapping tried in 2026-08-18 was mapping onto the wrong kind of operator, not just using wrong numbers. `DS_Fil_HDR_ColAdj` (tone-correct's variant) adds exactly 4 instructions/constants, consistent with (but not proof of) `TONE_CORRECT_BANK`'s four quantities.
  - **`g_BlendMode` decoded and shipped**: replaces the `_Edge`/`_Alp`/`_Add` filename heuristic, which disagreed with the engine on 153/584 materials (26%) — a live case-sensitivity bug, tag-less variants, every `A03_Sky*` rendering opaque, and four light-shaft/cloud materials rendering additive when the engine says alpha-blend. New `lightmap_sub.gdshader` covers the 139 real `a04_blood`-family meshes that had no prior route.
  - **`g_TexScroll_0` UV-scroll wired for every material family**: a new `vfx_scroll`/`vfx_scroll_add` shader pair for the 108 unlit scrolling standard-family meshes (`StandardMaterial3D` has no UV-animation hook at all), a `tex_scroll_0`/`_1` uniform for the lightmap/terrain-blend families (14 meshes). 25 *lit* scrolling standard meshes (lava/slime/`a05water03`) still don't scroll — not judged worth reimplementing Godot's PBR by hand for.
  - **Vertex colour multiplied into the diffuse result** for the lightmap and standard families (35% of map meshes carry real non-white vertex colour, confirmed corpus-wide — `shader/ds_flver.shaderbnd`'s `Col`-prefixed shader names and 140/176 real captured fragment programs both confirm it's a near-universal FromSoft convention, not occasional). Deliberately **not** applied to `terrain_blend.gdshader` — see docs/ARCHITECTURE.md's terrain-blend entry for why.
  - **User-confirmed in-editor (2026-08-27): scrolling and the new shaders for sky/light/fog materials look accurate.** Build clean, headless structural check confirmed every routing branch, and a real-driver (`opengl3`) check reported zero shader errors across all new/edited shaders before this testing.
  - Process note: a chunked corpus scan's earlier CoreCLR SIGSEGV was traced to system load (concurrent unrelated-process crashes on the same machine), not the workload — a full unchunked run completed cleanly post-restart. Not a code issue; no importer change resulted. See `.local-notes.md`.

- **Lightmap/drawparam system, part 15 — the dig's ranked items 1 and 2 implemented (2026-08-27): env-cubemap name resolution + `g_EnvSpcSlotNo` (data-only), and `LIGHT_BANK`'s second, larger hemisphere pair wired into the shared hemisphere shader.** Both verified headlessly end-to-end; the hemisphere change is the visually significant one and wants in-editor eyes.
  - **The load-bearing claim from part 14's ranking was checked first and is WRONG as stated.** "The lightmap/blend family is exclusively `g_LightingType=3` (HemEnv)" is really **160 of 192 (83%)**. The 32 exceptions are real: 22 are `g_LightingType=1`/HemDir3 (`C_Wet[DSB][M]_Skin`, `C_DullLeather[DSB][L]_Skin` — character materials that happen to carry a lightmap or blend tag — plus the map material `A07_[D][L]`), and 8 are `g_LightingType=0`/unlit (`A07_yuki` snow, already known as part of the unlit VFX family). Separately, 92 *standard*-family MTDs are `g_LightingType=3`. **Consequence for the roadmap:** the tidy "HemEnv = lightmap family, HemDir3 = standard family" split that made items 3 and 4 look mergeable is approximate, not exact, so whichever shader family gets the directional lights will need a real per-material `g_LightingType` gate rather than relying on which builder the material routed to. Also a live minor bug this exposed: those 8 unlit `A07_yuki` lightmap materials currently go through `BuildLightmapMaterial` and receive hemisphere/env lighting they should be skipping — small, not fixed this pass.
  - **The hemisphere-diffuse pair was wired, user-tested, and reverted the same day — a real regression, and the negative result is the valuable part.** `LIGHT_BANK` carries two hemisphere pairs and this project had only ever read the ambient one. The unread *diffuse* pair is 5-30x larger on real rows (Nexus row 0: ambient 0.05/0.07 vs diffuse 1.60/1.44; Boletaria B0: 0.10/0.10 vs 0.50/0.50), which made it look like a cheap fidelity win. Added as a second hemisphere term summed with the ambient one (intensity defaulting to `0.0`, the paramdef's own default, so unset rows were untouched), it **flattened the Nexus**: user-confirmed against a real RPCS3 reference screenshot, almost all shadowed areas disappeared and the scene read uniformly bright, losing the architectural depth the real game has. The mechanism is arithmetic, not calibration - a spatially *constant* term added to a scene whose only variation is the lightmap-scaled env term collapses contrast from `(0.05+3.00)/0.05 = 61x` to `(1.65+3.00)/1.65 = 2.8x`.
    - **The user spotted the clean natural experiment that confirms it**: the Old One area bundled into `m01` kept its known bright-white-ground problem essentially unchanged - and that row (`m01` row 3, `地下世界`) is precisely the one with `colA_du=0`, so it received no new light. The change did exactly what the data predicted, everywhere it applied.
    - **Two follow-up readings were tested against the data before reverting, and both fail** - worth not re-reaching for: (1) *lightmap-gating* the pair rather than adding it flat does restore contrast where the pair is set, but 105 of 186 referenced rows have `colA_du=0`, and those (the Old One included) would collapse to flat, near-black `1.0x` contrast; (2) *hemisphere-diffuse and the directional trio as alternative primary light sources* is disproven outright - Pearson r between their totals is **-0.15** (no relationship), and only 5 of 186 rows lack directional lighting entirely, so the directional trio is near-universal rather than a substitute for the hemisphere pair.
    - **Conclusion, CORRECTED after user review - and the correction is the most important part of this entry.** The first write-up concluded "this pair can't be landed alone, fold it into the directional-light work." That is the wrong instinct, and the user named it directly: *when we confirm something from DeS's own files but applying it looks wrong, the question isn't "what else should we add" or "revert and try something different" - it's "what does the DeS engine do that Godot doesn't, such that engine-accurate data renders wrong here?"* Applied to this case the answer is immediate and checkable: **this project has no transfer function anywhere** - no HDR target, no tonemap, no exposure, no `CameraAttributes` (verified by grep: nothing in `addons/archstone/` references any of them). The shaders compute a final colour straight into `ALBEDO`, so values >1.0 clip hard and values near 0 crush. With `m01` row 0's real numbers (`ambient 0.05 + lightmap x envDif 3.00`, mid-grey diffuse) **the top ~35% of the lightmap's range was already clipping before this term existed**; adding a constant 1.6 pushed most of the frame past 1.0. The observed "flattening" was clipping, not a wrong formula. DeS accumulates in HDR and resolves through `DS_Fil_HDR`'s per-channel 1D LUT built from `grayKeyValue` (0.18) + eye adaptation (`minAdaptedLum` 0.08 / `maxAdapredLum` 0.20) - a curve that compresses highlights *and* lifts shadows. **The pair should return once a transfer function exists.** The two reformulations tested above are still correctly ruled out as *substitutes* for that; they were attempts to reshape the data instead of fixing the output stage.
    - Process note: checking candidate reformulations against the *param data* (contrast arithmetic, correlation) before implementing them disproved both in minutes without a second round of user testing - that part was right. What was wrong was the framing that sent me looking at the data at all, when the discrepancy was on the Godot side.
  - **Two process lessons, both cheap and both new here.** (1) **Loading a `.gdshader` and calling `get_shader_uniform_list()` under plain `--headless` DOES catch Godot-shader-language errors** — it caught a real one this session (a local `vec3 diffuse` in the shared `#include` colliding with `lightmap_common.gdshaderinc`'s `uniform sampler2D diffuse`, breaking 4 of 5 shaders while `terrain_blend.gdshader` compiled fine because it names its samplers `diffuse1`/`diffuse2`). This does *not* contradict docs/ARCHITECTURE.md's "`--headless` never compiles GLSL": Godot's own shader parser runs at load and catches language-level errors, while GLSL compilation is a separate later stage that still needs the real-driver check. Cheap first gate, not a replacement. (2) **A GDScript check script that errors never reaches its `quit()`, so the SceneTree runs forever and looks exactly like a hang** — and `grep` in the pipeline buffers, so a hung run shows *zero* output even for lines already printed. Redirect to a file instead of piping when a check might hang, and sample `/proc/<pid>/stat` utime+stime to tell "stalled" from "working" (0 ticks/sec = stalled).
  - **A custom C# struct can't cross into GDScript at all** — `GetEnvCubemapNames` originally returned a `readonly record struct`, which made the method invisible to GDScript (`Invalid call. Nonexistent function`), i.e. unverifiable by this project's only test mechanism. Changed to `Godot.Collections.Dictionary`. Worth knowing before adding further data-only C# API: `PARAM.Row`-returning methods like `GetLightBankRow` have the same property and are simply never called from GDScript.

- **Lightmap/drawparam system, part 16 — a targeted research pass (2026-08-27) prompted by the user's methodological correction, which answered the "why does engine-accurate data look wrong in Godot" question with real bytecode rather than inference.** The user's framing, worth keeping as a standing rule: *when something is confirmed from DeS's own files but renders wrong here, ask what the DeS engine does that Godot doesn't — don't revert the finding or reach for more data.* Four results, three of which change planned work.
  - **RPCS3's shaderlog dump format changed** — it now writes `.spirv` files (Vulkan backend), but they are plain GLSL text (`#version 450`), not binary, so they read exactly like the old `.glsl` captures. 338 files, 228 real (zero interpreter-fallbacks in this capture, unlike earlier ones). The old `RSX_FP_OPCODE` filter still works. Don't assume the extension means binary SPIR-V. **Vulkan is now the preferred capture mode** (user, 2026-08-27): OpenGL mode has performance problems in RPCS3, so Vulkan is both faster *and* still yields readable shader text.
  - **Capture provenance, which bounds how far these numbers generalize** (user, 2026-08-27): the dumps come almost entirely from **`m01` (the Nexus, mainly the central hub — not the Old One / ending pieces) and `m02` (Boletarian Palace, mainly `m02_00`)**. These two are the standing test grounds because they're two visually distinct environment types reachable early without progression. So "44 HemEnv shaders / 43 HemDir3" describes those two areas' materials, not a whole-game census — treat family *ratios* as indicative, and the *structural* findings (which terms exist, what feeds what) as general. The user will flag it if captures from other areas ever get added.
  - **The map/lightmap family genuinely has NO directional lights — confirmed, and it kills a planned change.** Cross-tabulating the hemisphere-blend signature (`(N.y + c) / 2.`) against directional `dot(normal, constant)` terms across all 228 real shaders: **44 shaders have the hemisphere blend with zero directional terms** (HemEnv), 43 have hemisphere + directional (HemDir3). Directional-term counts are **only ever 0, 3, or 6, never 1 or 2** — 3 = three diffuse N·L, 6 = three diffuse plus three specular R·L, exactly matching `MTD.LightingType.HemDirDifSpcx3`. So adding `LIGHT_BANK`'s directional trio to the lightmap shader would be adding light the real engine does not apply to those materials. The trio belongs to `g_LightingType=1` (chr/parts/obj) only. A plan to "fix the waxy walls with directional lights" was dropped on this evidence.
  - **The real cause of the flat/"waxy" look, and it needs no new lighting data at all.** A real HemEnv shader (`FragmentProgram124`) reconstructs a full per-pixel normal from the bumpmap - samples a 2-channel tangent-space normal, rebuilds Z as `sqrt(1 - (x²+y²))`, builds a TBN basis by cross product, transforms to world space - and then uses that per-pixel normal `h6` for **both** the hemisphere blend factor (`(h6.y + c) / 2.`) **and** the env cubemap reflection vector (`TEX3D(11, h6.xyz)` off a reflection built from `h6`). This project's shaders do neither: `hemisphere_ambient()` blends on `world_normal`, a varying computed from the *vertex* normal in `vertex()`, and the env term is a flat lightmap sample with no normal dependence whatsoever. `NORMAL_MAP` is assigned but only feeds Godot's own lighting response, and there are no lights in the scene - so **every lighting term in the lightmap family is currently blind to the normal map**, which is exactly why engraved relief disappears. Fix is per-pixel normals in the hemisphere term, not more light sources.
  - **The tonemap operator is reconstructable analytically - no runtime LUT capture needed.** `tonemapbank.paramdef`'s own descriptions: `grayKeyValue` "larger → screen brighter, smaller → darker"; `minAdaptedLum` "make smaller to adapt to seeing in dark places"; `maxAdapredLum` "make larger to adapt to bright places"; `adaptSpeed` "speed of light/dark adaptation"; `bloomBegin` "starts to bleed when luminance exceeds the threshold"; `bloomMul` "multiplied above the threshold (0 = no bleed)". That is Reinhard 2002 photographic tone reproduction with a temporally-adapted luminance clamped to a `[min, max]` window - so `DS_Fil_HDR`'s 1D LUT is a tabulation of a curve we can rebuild from authored values. Supersedes docs/DIG-era "needs runtime instrumentation" framing for the *operator*; the LUT's exact contents still aren't captured, but no longer need to be.
  - **Godot's Compatibility (opengl3) renderer is RGBA8 low-dynamic-range with no HDR viewport or HDR output** (per Godot's own renderers doc). This settles an architectural fork: a `CanvasLayer`/`hint_screen_texture` post-process pass **cannot** recover clipped highlights, because clipping already happened in the 8-bit framebuffer. Any DeS transfer function has to be applied **in-shader, before writing `ALBEDO`** - which suits this project, since these shaders already hand-compute a final colour. (An older Godot status article also states the OpenGL3 renderer works in sRGB space rather than linear, unlike the Vulkan renderers; not confirmed against current docs, worth verifying separately before relying on it.)
  - **Incidental independent confirmation:** a real terrain-blend shader (`FragmentProgram11`) does `fma(diff_color.wwww, layerB - layerA, layerA)` - i.e. the two diffuse layers are lerped by **vertex-colour alpha**, confirming from engine bytecode the `COLOR.a`-is-the-blend-weight finding this project derived statistically back in the terrain-blend work.

- **Lightmap/drawparam system, part 17 — env cubemap decoding built and verified (2026-08-27), the prerequisite slice for a real env term. No shader consumer yet, so no visual change.** Chosen as the first implementation step because it was the one genuine unknown gating everything else, and it carries zero visual risk.
  - **Pfim silently decodes only face 0 of a cubemap.** Handed a valid 6-face DDS from `Headerizer`, it returns one face plus its mip chain (a 64x64 `m01` cubemap came back as 21952 bytes = exactly one face + mips) with no error. Worked around by re-wrapping each face as its own single-image DDS: copy the 128-byte header, zero `dwCaps2` (offset 112) to clear the `CUBEMAP_ALLFACES` flags, append that face's slice, decode. Face stride is always an exact sixth of the headerized payload - verified on both real layouts (`m01` block-compressed with per-face mips, 2744B; `m02` uncompressed 32x32, 4096B). This reuses Pfim's DXT decoding instead of reimplementing block decompression.
  - **Cubemap faces decode to two different Pfim formats in real data** - `Rgba32` for `m01`, `Rgb24` for `m02`. The existing 2D `DecodeTexture` throws on anything but `Rgba32`, so a naive reuse of that path would have rejected half the corpus.
  - **`Cubemap.get_layer_data()` returns blank images under the Compatibility renderer** - confirmed with a control: a cubemap built in pure GDScript from six known solid colours also read back as `0.00` on every face. So it is a Godot readback limitation, not a decode bug, and **cubemap contents cannot be verified by reading them back** in this project at all. The working verification is to *render* with them: bind to a `samplerCube` uniform, sample the six axis directions across a `ColorRect`, and read the rendered viewport image. That returned non-zero, per-face-distinct values whose extremes matched the pure-C# harness (face 4 brightest, face 3 darkest), and it doubles as proof that **`samplerCube` binding works in the Compatibility renderer** - which the eventual env-term shader depends on.
  - **Wired into the shaders the same session (awaiting in-editor testing).** Per-pixel normals + both env cubemaps now drive the lightmap and terrain-blend families, following the captured HemEnv structure: EnvDif sampled by the surface normal, EnvSpc by the reflection vector, both gated by the lightmap (standing in for the engine's `min(shadow, lightmap)` since no shadow map exists). Verified: build clean, zero shader errors under a real `opengl3` driver across all 8 shaders, and on a real Nexus load **335 of 335 surfaces bound a real per-situation cubemap with zero white fallbacks**, with `use_normal_map` true on 225 and false on 110 (the materials that genuinely ship no bumpmap). **Expected visual direction, worth stating before testing:** the env term changes from `lightmap × 3.0` to `lightmap × cubemap × 3.0`, and real cubemap samples run ~0.1-0.5, so the env contribution drops roughly 3-5x. Scenes should get **darker**, not brighter — the opposite of the hemisphere-diffuse regression. Given that the previous formula clipped the top ~35% of the lightmap range, some of that reduction is expected to be correct rather than a loss.
  - **Open question found in passing, worth settling before wiring the slots:** `EnvSpc_m02_003`'s six faces are nearly uniform (means 72-74, i.e. heavily blurred), while the earlier `g_EnvSpcSlotNo` survey found slot 3 materials have the *highest* median `g_SpecularPower` (60, i.e. sharpest/glossiest). A sharp material sampling the blurriest cubemap is backwards, so either the slot→roughness ordering is inverted from the earlier reading or the slots mean something else. Check this against the real cubemaps before trusting the "slots 1→2→3 track sharpness" note in docs/ARCHITECTURE.md.

- **Lightmap/drawparam system, part 18 — the full DeS material/lighting pipeline traced end-to-end from real bytecode (2026-08-27), at the user's request to establish engine accuracy as the standard rather than tuning toward what looks right.** The reconstructed pipeline now lives in docs/ARCHITECTURE.md's "The DeS rendering pipeline, reconstructed" section; this entry keeps the trail and the corrections.
  - **Reference shader is `FragmentProgram151`** — a lightmapped HemEnv map piece (samplers: `tex0` diffuse @ `tc6.xy`, `tex3` second layer @ `tc6.zw`, `tex6` **lightmap @ `tc7.xy`** i.e. its own UV set, `tex7` shadow, `tex11` EnvDif cubemap). Cross-checked against `FragmentProgram124` (bumpmapped HemEnv with EnvSpc) and `FragmentProgram11` (HemDir3 terrain blend).
  - **`min(shadow_attenuation, lightmap)` confirmed literally** — line `h0.xyz = min(h5, r3)` where `h5` is the shadow term and `r3` the lightmap sample, feeding `fma(h1, h0, h4)` = `EnvDif * gate + hemisphere`. This upgrades the earlier "the env term is gated by min(shadow, lightmap)" note from a partial read to a confirmed, fully-traced composition.
  - **A correction to an earlier claim in this file:** `FragmentProgram124` was described as having "no cube, no shadow". It has both (`tex11`/`tex12` cubemaps, `tex7` shadow) — the census loop that produced that label printed sampler2D counts but never actually filtered on cube/shadow. The structural findings drawn from it (per-pixel normal reconstruction, EnvDif-by-normal, EnvSpc-by-reflection) are unaffected and were re-verified here.
  - **The finding that matters most: every material shader ends with an exposure normalize.** `output = precision_clamp(_builtin_div(result * k, exposure_constant), 0.0, 1.0)` — present in **190 of 219** real shaders. DeS packs HDR-range lighting into a `[0,1]` target *inside the material shader*, and only then runs the post chain (adaptation → bloom → per-channel tone LUT). This explains the whole "too bright"/"too dark" history: absolute brightness is set by a divide and a LUT that this project has never had, so raw material output reading wrong in either direction says nothing about whether the shading math is right.
  - **It also removes what looked like a compromise.** Godot's Compatibility renderer is RGBA8/LDR and can't post-process HDR, which had made an in-shader transfer function look like a workaround. It isn't — DeS does its exposure step in the material shader too. The engine-accurate route and the only Godot-feasible route are the same route.
  - **Fog is confirmed per-material, not scene-wide** — `result * tc8 + tc9`, fog factor and colour both interpolated per-vertex and applied per-pixel, in every material shader. Supports the existing architectural note that `FogID` should drive a per-material term rather than a `WorldEnvironment` fog node.
  - **Unresolved detail seen in the trace, not chased:** `FragmentProgram151` lerps its result toward a constant colour by a factor derived from the **EnvDif cubemap's alpha channel** (`h1.w = clamp(EnvDif * c19).w * c24`, then `fma(h1.w, c38 - result, result)`). Possibly the `HemEnvLerp` lighting model from the shader-name taxonomy. Our cubemap decode keeps alpha, so this stays implementable later.

- **Lightmap/drawparam system, part 19 — the whole output stage built and wired (2026-08-27): per-material fog, the transfer function, and per-channel tone correction, plus the discovery that these shaders were never really being lit by anything we controlled.** Landed as one unit because all three are per-material stages in the real engine and a half-built transfer function isn't testable.
  - **The finding that reframes every earlier brightness judgement: the lightmap family's shaders were lit by the 3D editor viewport's default preview sun and sky.** They were ordinary lit `spatial` shaders writing `ALBEDO`/`EMISSION`, in a scene with no light nodes and no `WorldEnvironment` — under which Godot's diffuse contribution is zero and only `EMISSION` survives. Since the maps visibly showed diffuse texture, something *was* lighting them, and the editor's preview environment is the only candidate. That means an arbitrary, blue-tinted sky ambient was scaling everything, and **every "too bright"/"too dark"/"too cool" reading in parts 15-18 was taken through it** — including the user's own observation that the editor look leans white/blue against RPCS3's warmer cast. Fixed by making the whole family (plus `terrain_blend.gdshader`) `unshaded`, which is what DeS does: its material shaders emit a finished pixel. `EMISSION`/`NORMAL_MAP`/`METALLIC` are gone and the envSpc term is summed straight in. **Worth carrying forward as a general trap: a Godot shader that hand-computes a final colour must say `unshaded`, or the editor silently becomes an uncontrolled input to it.**
  - **The transfer function collapses DeS's two output steps into one curve, on purpose.** The engine divides by a per-shader exposure constant in the material shader, then runs `DS_Fil_HDR` — a 1D LUT tabulating Reinhard 2002 — over the result. Since the LUT *is* that curve, the constant cancels and evaluating the curve directly on the HDR value is the composition of both. That also sidesteps needing the constant, which the dumps don't expose. Per channel, not on luminance, because a 1D LUT is by construction a per-channel curve.
  - **One value in the output stage is not authored data: adapted luminance.** The engine measures the frame's own average luminance and clamps it into `[minAdaptedLum, maxAdapredLum]`; Compatibility can't read frame luminance back. `FlverLoader` passes `minAdaptedLum` (`m01`: 0.08) as the value a dark interior would settle at, kept as a uniform so a bright exterior can be given the other end. Everything else — fog distances/colour/density, `grayKeyValue`, brightness/contrast/saturation/hue — is read straight from the banks.
  - **The bank data says the missing warmth is real and was reachable all along.** `m01`'s tone-correct row 0 is `brightness (0.80, 0.87, 0.83)`, `contrast 1.1`, `saturation 0.9` — relative to its own maximum that is a green-leaning, slightly desaturated cast, which is the direction the user described RPCS3 as leaning. **189 of 209 `m01` parts use `ToneCorrectID=0`** and 197 use `ToneMapID=0`, so this is the Nexus proper, not an edge row. Fog row 0 is literally named `神殿部分` ("temple section"): begin 10, end 220, colour `(54, 50, 120)` — the blue haze visible in the RPCS3 reference.
  - **Verified before hand-off, under a real `opengl3` driver:** all 8 shaders compile, and on a real `m01` load **335 of 335 surfaces bound output-stage uniforms** alongside the light-bank ones. Rendered stills from inside the Nexus (see the camera trick below) show engraved wall relief clearly readable and shadowed areas holding their contrast — the "waxy" complaint from part 16 — with a warm-neutral rather than blue-white cast.
  - **A cheap trick worth reusing: place check-script cameras at `MSBD.Parts.Objects` positions.** Map pieces bake their world transform into their vertices, so every map-piece node sits at `(0,0,0)` and any centroid/AABB of them is meaningless (a first attempt put the camera 2000 units below the level). Object placements — doors, levers, props — carry real positions at real floor level in reachable areas, so sorting them by Y and sampling a few gives usable interior viewpoints with no manual coordinate hunting.
  - **A second trick, for separating "the shading is dark" from "the output stage is darkening it":** set `fog_density` to 0 and `tone_key` to whatever `tone_adapted_lum` is. That makes the Reinhard scale exactly 1, and for the small values these shaders produce `x/(1+x) ≈ x`, so the frame reads back as its own pre-output-stage input. No debug uniform needed in the shipped shader.
  - **Still open, seen in these renders:** the distant Old One landscape shell still blows out toward white. Its `FogID=1` row has `colA=200` (a 2.0x colour multiplier) on a near-white fog colour, and its `LightID=3` row has `env_intensity=4.5` — so it is bright by authored intent, and the missing shadow half of `min(shadow, lightmap)` is the most likely reason nothing is pulling it back. Not chased.

- **Lightmap/drawparam system, part 20 — the Nexus's fog is atmospheric scattering, not `FOG_BANK`'s colour (2026-08-28), found by tracing the *vertex* programs after the user reported the fog reading far too purple.** The user's framing was right and worth keeping: real data rendering wrong means our composition is incomplete, not that the data needs adjusting.
  - **A hypothesis worth recording because it was wrong: `source_color` is a no-op under Compatibility.** The obvious suspect was Godot applying an sRGB→linear conversion to `fog_color : source_color`, which would have expanded the channel ratios and over-saturated the violet. Tested directly — the same authored value bound through a `: source_color` uniform and through a plain `vec3` renders **identically**, at the authored B/R ratio (2.26 vs 2.22 authored). So hinted colour uniforms need no special handling here, and the earlier unverified note that "the OpenGL3 renderer works in sRGB space rather than linear" is consistent with this. Don't re-litigate it.
  - **`VertexProgram*.spirv` is where the fog actually lives, and it is not a lerp.** `tc8 = dst_reg15`, `tc9 = dst_reg6` in 67 of 110 vertex programs; the computation is a per-channel `exp2()` producing a **vec3** transmittance, plus a separate in-scatter colour built from two summed light contributions gated by `(1 - transmittance)`. With the fragment side's `result * tc8 + tc9`, that is extinction-plus-in-scatter — **Hoffman & Preetham's real-time outdoor light scattering**. Lesson for future traces: the fragment programs alone were never going to show this; the interesting per-vertex work was one file type away the whole time.
  - **`LIGHT_SCATTERING_BANK` holds the model's parameters under the paper's own names**, which is what makes the identification certain rather than a guess: `lsBetaRay` ("Rayleigh scattering coefficient (air molecules)"), `lsBetaMie` ("Mie scattering coefficient (fine particles)"), `lsHGg` ("scattering direction coefficient, -1 backward, 1 forward" — Henyey-Greenstein), `inscatteringMul`, `distanceMul`, `blendCoef` ("scattering's degree of involvement, 100 = max"), plus `sunRotX/Y` + `sunR/G/B/A` and `reflectanceR/G/B/A` ("ground diffuse reflected light colour"). Addressed by `MSBD.Part.ScatterID`, parsed by `SoulsFormatsNEXT` all along and never read.
  - **The decisive number: `m01` scattering row 0 is named `神殿部分` — the same name as `FOG_BANK` row 0 and `LIGHT_BANK` row 0 — and sets `blendCoef=100` with a pure white sun at 200% and `lsBetaRay=6.0`.** So the Nexus's haze is white light Rayleigh-scattered, and `FOG_BANK`'s violet `(54,50,120)` is blended out to nothing there. We were rendering 100% of the half that should contribute 0%. Verified after wiring: **200 of 335 Nexus surfaces resolve to exactly that row** (`blend=1.0`, `betaRay=6.0`, white sun), and 4 distinct scatter rows appear across the map.
  - **Row 2 (`縦穴`, the vertical shaft) is the control case**: `blendCoef=0`, i.e. pure `FOG_BANK` fog with no scattering. Both banks are real and both are needed — this isn't a case of one superseding the other.

- **Lightmap/drawparam system, part 21 — the scattering model corrected against a full vertex-program trace (2026-08-28), after a side-by-side against a real RPCS3 Nexus frame showed the atmosphere converging but not there.** Three of the four corrections came straight out of the bytecode; the fourth is an openly fitted constant.
  - **Distance is the raw radial camera distance, not a normalized ramp.** `VertexProgram1` builds `r0.w = dot(V,V)`, `r2.y = inversesqrt(r0.w)`, then `r1.y = 1.0 / r2.y` — recovering `|V|` explicitly — and feeds that straight to the exponent. Part 20 had normalized it against `FOG_BANK`'s `fogBeginZ`/`fogEndZ` ramp and clamped to `[0,1]`, which saturates the curve almost immediately and **destroys the per-channel split that is the entire reason scattered light is blue.** This was the piece flagged as "inferred rather than read" at the time, and it was the wrong inference.
  - **~~The two phase terms are summed with no division by `beta_total`.~~ RETRACTED — see part 22.** The claim was that the divide flattens the wavelength dependence, on the evidence that the *phase term alone* comes out within 6% across channels with it. That test was the wrong one: at low optical depth `(1 - Fex) ~= betaTotal * d * k`, so the normalization **cancels against it** and the near/mid-field hue is identical either way. The divide only sets the far-field ceiling — where it makes thick haze desaturate toward neutral, which is what the reference actually shows. Removing it produced a ceiling of B/R ~3.3 against the reference's 1.44-1.49.
  - **`blendCoef` is a strength dial on the scattering, not a crossfade against `FOG_BANK`.** `tc8 = 1 + (Fex * c105.xyz - 1) * c110.w` is `mix(1, Fex*c105, blend)` — it lerps the transmittance from "no atmosphere" toward the computed one, and scales the in-scatter by the same factor. Part 20 read it as choosing between the two banks. **How `FOG_BANK` itself reconciles with this is still genuinely unresolved** — every captured material shader carries a complete but never-invoked fixed-function fog path, which is exactly the shape `FOG_BANK`'s begin/end/colour fits, so it may not reach these materials at all. Fading the linear fog out by the same `blendCoef` is what currently matches the reference; it is a reading, not a finding.
  - **Two constants are unrecoverable and are folded into one fitted uniform (`scatter_distance_scale`).** The distance passes through two literal registers before `exp2`, and the in-scatter takes a further colour multiply (`c110.xyz`) that couldn't be attributed to a bank field. Literal register values don't survive the dump. **Fitted by measuring the RPCS3 frame directly rather than by eye:** its mid-distance band sits at B/R 1.44 and its far band at 1.49, and solving the model for that ratio at a ~200-unit sightline gives ~2.5e-5. A first guess of 1e-3 was ~40x too strong and turned everything past a few metres saturated blue.
  - **Measuring the reference images is cheap and worth doing before tuning anything.** Cropping matching bands out of the RPCS3 frame and the editor frame and printing mean RGB took one short script and turned "the fog looks off" into a number to solve for. It also separated two problems that look like one: **the hue was already close in the far field (1.45 vs 1.49) and wrong in the mid field (1.15 vs 1.44), while the overall level is short by roughly half in both** (mean R 0.088 vs 0.136). The hue is what this entry fixes; the level is the separate still-open "shadows too dark" item, and additive in-scatter only partly offsets it.
  - **`exp2`, not `exp`** — the real instruction is `EX2`, which folds a factor of `ln 2` into the scale. Immaterial on its own since the scale is fitted, but it keeps the curve shape honest.

- **Lightmap/drawparam system, part 22 — the part-21 scattering rewrite was a user-confirmed regression (2026-08-28), and the post-mortem matters more than the bug.** Reported symptoms: at normal player positions inside the map the fog is essentially invisible; flying far out, the density appears but as an intense saturated blue rather than the reference's lighter blue.
  - **The root cause was fitting one parameter against the wrong observable.** `scatter_distance_scale` is a *density* knob; it was fitted against a *hue* target (the measured B/R ratio). Because the in-scatter's saturation ceiling was independently ~2.2x too high, the only way the fit could hit the hue target was to crush the density by ~40x until the small amount of over-blue fog averaged out correctly. Both errors were real, and they were cancelling in the mean. **Fitting N knobs against fewer than N independent observables will silently produce this**: a number that satisfies the metric and is wrong in every particular.
  - **Concretely:** at 220 units the blue channel reached only ~4% opacity, against the previous build where the clamped `FOG_BANK` ramp guaranteed full strength by `fogEndZ = 220`. That is the whole "fog invisible at player range" report.
  - **The saturation ceiling is the separable half.** As `(1 - Fex)` saturates, the in-scatter tends to `betaRay * P_ray + betaMie * P_mie`, whose own B/R is ~3.3 with the wavelength weights in use. The reference's far band is 1.49. So the ceiling is wrong independently of any distance scale, and it is set by two things: whether the `/betaTotal` divide is present (see the part-21 retraction), and the magnitude of the per-channel split.
  - **The wavelength weights were an import, not a measurement — the least-defended value in the file.** `c104` being read as `.xyzx` rather than splatted `.xxxx` proves a per-channel split *exists*; it says nothing about magnitude. Textbook lambda^-4 coefficients at 650/570/475nm carry a B/R of 3.5 all by themselves, and were dropped in without ever being checked against the engine.
  - **Standing lesson, worth applying beyond this:** when a reading from real data renders wrong, the failure has been *composition* every time so far (part 20's fog-vs-scattering, part 21's normalization, this entry's fitting) — never the authored data. But "the data is right" does not license fitting a constant to make the picture match; that just moves the error somewhere unmeasured.

- **The shader library is a fully-named, complete material system, and it had never been opened (2026-08-28).** `PS3_GAME/USRDIR/shader/` holds five `.shaderbnd` files; `ds_flver.shaderbnd` alone is 4.6MB and **1349 entries** (134 `.vpo` vertex programs, 1215 `.fpo` fragment programs), each carrying its original build path `N:\DemonsSoul\Source\Shader\DS_Flver\Debug\<name>`. Plain `BND3.Read`, no special handling. **`shader` is not in `AssetExtractor.KnownCategories`, so none of this is mounted** — it was reachable only from the raw game directory.
  - **The names are a fixed-width field layout that spells out the whole system**: `DS_<family>_<textures>_<shadow>_<lightingModel>.fpo`, `_`-padded to fixed columns. Families: `Phn` 626, `Gst` 576, `Dbg` 9, `Water` 3, `Ghost` 1. Textures: `Dif` -> `DifSpc` -> `DifSpcBmp` -> `+Mul` -> `+Lit` (and `+LitSdw`/`+LitCsd`). Shadow column: `Sdw` (shadow map) and `Csd` (cascaded) as **separate compiled shaders**, 400 of them.
  - **The complete lighting-model list, no longer inferred: `HemDir3`, `HemEnv`, `HemEnvLerp`**, each crossed with `{ -, PntS, PntSS, PntSSSS }`, plus a bare `Non` / `Pnt` / `PntS` / `PntSS` / `PntSSS` / `PntSSSS` set. Four shaders per cell.
  - **`HemEnvLerp` is confirmed as a real named model** - the loose end from part 18 (`FragmentProgram151` lerping its result toward a constant colour by the EnvDif cubemap's alpha). It has a name and a full variant set, so it is a first-class lighting model, not an edge case.
  - **Point lights reach map geometry.** `HemEnv` - the map/lightmap family - has `PntS`/`PntSS`/`PntSSSS` variants, so map surfaces genuinely receive 1/2/4 point lights. This project's docs previously recorded `POINT_LIGHT_BANK` as a fixture-preset table with no known consumer. It has one. Direct bearing on the standing "shadowed geometry too dark" report, since the Nexus's braziers and torches are authored lights we apply none of.
  - **MTD -> shader is a direct, authored mapping, not a heuristic.** Every `.mtd` carries a `ShaderPath` naming its `.spx` (32 distinct values across 612 mounted MTDs), and those map 1:1 onto the fpo names: `DS_Phn_ColDifSpcBmpMulLit.spx` -> `DS_Phn_DifSpcBmpMulLit_<shadow>_<model>.fpo`. The material fixes family and texture set; the runtime picks the shadow variant and lighting model. The `Col` prefix is the vertex-colour multiply this project already implements.
  - **Every MTD also carries a dev-written Japanese `Description`, never read until now.** Example: `m_9glass[dsb][ml]_edge.mtd` -> "マップ用　【ガラス、陶器マテリアル】スペキュラー / バンプ / マルチテクスチャ / ライトテクスチャ / α抜き" ("for maps [glass/ceramic material], specular / bump / multi-texture / light texture / alpha-cutout"). This independently confirms, in the authors' own words, routing this project previously derived statistically: **`Mul` = multi-texture (the terrain blend), `Lit` = light texture (the lightmap), `α抜き` = alpha cutout, `半透明` = semi-transparent.**
  - **Symbols are stripped - there is no parameter name table.** A `.vpo` is raw RSX microcode behind a `FF 0F 00 11` header; an ASCII scan of `DS_Phn_PINTT_DDL_Sdw.vpo` returns only byte noise. So the shader files do **not** hand over the uniform register names, and `c104`-`c110` (CPU-supplied per draw) are not in them.
  - **But the embedded literal pool is there, and it closes half the fog question.** A big-endian float scan finds a small constant block (0x1E70 in the `Sdw` variant, 0x1D50 in `Non`) holding two vec4s: `(3.0, 0.49804688, 2.0078433, 0.0)` and **`(0.0009765625, 1.4426949, 1.442695, 1.0)` = `(1/1024, 1/ln2, 1/ln2, 1.0)`**. The second is `c466`, corroborated by `c466.w = 1.0` having been derived independently from the trace (used as both `1 + cos^2 theta` and `1 - Fex`). **So the two "unknown scale literals" in the distance chain are `1/ln2` twice - pure `exp`->`exp2` unit conversion carrying no hidden magnitude.**
  - **Which collapses the remaining unknown to exactly one vec3.** Everything left in the scattering chain lives in `c104.xyz`, CPU-computed from `lsBetaRay`/`lsBetaMie`. The "wavelength split" and the "distance scale" were never two unknowns - they are the same vector, which is precisely why part 22's one-knob fit failed the way it did. One vec3 against two independent observables (far-field hue ceiling, near-field density onset) is solvable; it is also exactly what an RSX transform-constant capture would print, which makes that a narrow ask rather than a fishing trip.
  - **Method note worth generalizing:** the first two floats of the other vec4 decode as `127.5/256` and `512/255`, the standard unsigned-byte-to-signed-float vertex-attribute decode constants. Recognizing literal pools by their *values* is a cheap way to identify what a stripped shader is doing without any disassembler.

- **Shader library mounted and indexed (2026-08-28) — the follow-ups from the dig entry above, both landed.**
  - **Adding `shader` to `KnownCategories` alone would have looked like it worked and produced nothing.** These entry names are build paths (`N:\DemonsSoul\Source\Shader\DS_Flver\Debug\...`) with no `data/DVDROOT` segment, and `ResolveEntryOutputPath` returns `null` for those — which `WriteEntry` dropped **silently**, no warning, no error. A `FallbackEntryOutputPath` now mirrors the container's own relative directory and nests entries under a folder named after the container. Worth remembering as a latent trap for any future category whose containers don't use the DVDROOT convention.
  - **Both the plain and `.dcx` copies of a container decompress to the same binder**, so the first pass produced doubled output (`ds_flver/` *and* `ds_flver.shaderbnd/`). Stripping a trailing `.dcx` before taking the basename fixes it; the second pass reports `extracted=1421 skipped=1421`, the skips being the `.dcx` half correctly recognized as already current.
  - **Parse the names by feature substring, not by splitting on `_`.** The names are padded to fixed columns, so the same feature run appears fused (`DS_Gst_DifSpcBmpMulLitCsd_HemDir3`) or split (`DS_Gst_DifSpcBmpMul_Csd_HemDir3`) depending on where the columns fall — token counts run 2 to 6 across the corpus for structurally identical names. Substring detection reads both; splitting does not.
  - **Verified end to end: all 1215 `.fpo` indexed across five families, and 612 of 612 mounted MTDs resolve to a non-empty shader set, zero unresolved.** The bare `DS_Ghost.fpo` (two tokens, no feature or model field) needed the parser to treat both tail fields as optional; it is the shader for the 28 `DS_Ghost_Param.spx` materials, the ghost/dissolve family this project still doesn't handle.
  - **The most useful thing learned is a negative result: the material does not determine the lighting model.** `DS_Phn_ColDifSpcBmp.spx` (104 MTDs) and `DS_Phn_ColDifSpcBmpMulLit.spx` resolve to the *same* 13 lighting models. So HemEnv-vs-HemDir3 is a pure runtime choice, not derivable from the `.spx` name, and `MTD.LightingType` stays the only authored signal. This kills a plausible-looking shortcut for the `g_LightingType` routing work before anyone spends a session on it.
  - **Water has three shaders, not one** — `DS_Water_Env` / `Mask` / `Reflect`. This project implements a single `water.gdshader`; the other two variants are unexamined.

- **Material pipeline restructured onto the shader library (2026-08-28) - routing off authored data, one shader family for both lighting models, and the black-props population finally lit.**
  - **The classification switch was verified before it was made, not after.** Comparing the old bracket-tag/slot heuristics against the authored family+feature routing across all 612 MTDs gave **610 identical, 2 different** - both the magic barrier, whose `.spx` is `DS_Phn_ColDifMul` (a real two-layer material) but whose name carries no `[M]`/`[ML]` tag. Zero FLVER map materials reference it, so the authored routing fixes a latent bug with no visible effect. **Running the comparison first is what made this safe to land in the same pass as a much riskier change** - a refactor that provably changes 2 of 612 classifications is a different risk from one that might change any of them.
  - **The old heuristics are kept as a fallback**, not deleted: an `.mtd` that can't be read yields an empty family, and without the fallback such a material would silently lose its lightmap or blend routing entirely.
  - **`ResolveMtdShading` is now on the per-mesh path, so its `MTD.Read` is cached.** `ClassifyMaterial` calls it, and `ClassifyMaterial` runs once per mesh as well as once per material.
  - **HemEnv and HemDir3 share one shader family via a `lighting_model` uniform.** The engine ships a separate compiled binary per combination (1215 of them); mirroring that in Godot would mean over a thousand `.gdshader` files compiled at load. A uniform branch makes the same choice at the same point in the pipeline - the material fixes family and texture set, the runtime picks the model - and is the only thing Godot can express, since it has no material-driven preprocessor.
  - **HemDir3 deliberately takes no environment term.** Its name carries no `Env` and `MTD.LightingType.HemDirDifSpcx3` lists none. That is both engine-accurate and defensive: a material with no lightmap has its gate default to white, so an env term would arrive at full `env_intensity` (1.5x-5x on real rows) and blow out - the exact overexposure already documented for the lightmap family.
  - **Verified on a real m01 load: 75 surfaces route to HemDir3, all 75 with directional lights bound**, and all 8 shaders compile under a real `opengl3` driver. A sample row binds light 0 at `(2.60, 2.44, 1.87)` with two unused siblings - so the "three directional lights" are three *slots*, commonly with only one in use, not three lights always on.
  - **A gap found while checking coverage and deliberately not closed: `g_LightingType=3` with no lightmap still falls through to `StandardMaterial3D`.** Routing it to HemEnv is the accurate answer but would land `env_intensity` unattenuated against a white default gate. Left alone rather than fixed blind, since the session's other changes already need visual confirmation.
  - **Water joined the output stage and went `unshaded`.** Its old `ROUGHNESS`/`SPECULAR` sun-glint was being lit by the editor's preview sun - the same uncontrolled input the map shaders were freed from earlier in the session, still present in one file. `FlverLoader` now gates the light-bank and output-stage uniform groups independently, so a shader can take the atmosphere without taking ambient uniforms it has no use for.

- **Part 23 — the props stayed black after the HemDir3 work, and the cause was the tone-correct contrast formula crushing them (2026-08-28).** User-reported with the editor sun and WorldEnvironment off, as they normally run. Worth recording as a debugging trail because the first three suspects were all wrong and cheap to eliminate.
  - **Eliminated first, by rendering rather than reasoning: `vec3` array uniforms and `int` uniforms both reach the shader fine.** `get_shader_parameter()` only reads back what was *stored*, so it proves nothing about what the shader receives — the same trap as the `source_color`/`Vector3` no-op already in this file. A one-quad test outputting `arr[0]` and `float(model)` returned the exact set values. So `dir_light_colors`/`dir_light_directions`/`lighting_model` were never the problem.
  - **Eliminated second: the material state was completely correct.** Dumping all 75 HemDir3 surfaces on a real `m01` load showed real diffuse textures bound (256x1024, 1024x1024), ambient at the map's authored 0.05/0.07, three real directional colours and unit-length directions. Nothing was unbound.
  - **The actual cause was `des_tone_correct`'s contrast, which had a black point.** `(x - 0.5) * contrast + 0.5` with m01 row 0's authored brightness 0.80 and contrast 1.1 clamps everything below a post-tonemap **0.057** to pure black — a scene value of **0.0268**. A prop face lit only by that map's ambient (mid-grey texture 0.30 x ambient 0.05) sits at **0.0150**, under the black point. So every prop face not directly catching one of the three directional lights was clamped to zero *after* being lit correctly.
  - **~~The fix is read from the shader, not guessed~~ — the inference was INVALID, see part 26.** The reasoning was that `DS_Fil_HDR_ColAdj.fpo` has no float literals, so a pivot-and-scale contrast (which needs a `0.5`) is ruled out, leaving a gamma-style `pow(x, contrast)`. The premise is true and the conclusion does not follow: the file has no literals because **the entire tone curve is a CPU-baked 1D LUT**, which says nothing about the contrast formula's shape. `pow` was changed on invalid grounds; it was then kept on the narrower basis that the pivot form is *demonstrably* harmful (it clamps real scene values to zero) while `pow` is merely unverified — **later reverted to pivot anyway once the engine-accurate scattering haze covered that black point, see part 33.**
  - **This is very likely also the cause of the separately-reported "shadowed map geometry is too dark to make out detail".** That was attributed to missing point lights, and point lights are real (the shader library confirms `HemEnvPntS/SS/SSSS`), but they were never needed to explain it: the same black point crushes every shadowed map value below 0.027, and the Nexus's shadowed surfaces sit right in that range. **Re-check that report before spending a session on point-light binding.**
  - **Standing lesson, third instance this session:** the failure was in code written from an unverified assumption about the *shape* of an operation, not in the authored data or in the pieces that were carefully traced. Both this and the part-22 scattering regression came from filling a gap with a plausible textbook formula instead of checking what the engine's own shader could and couldn't contain.

- **Part 24 — the rainbow-speckle regression across most maps was the uncompressed cubemap path decoding ARGB as RGB24 (2026-08-28).** User-reported as "neon paint and coloured noise" flat on materials plus "broken reflections" that move with the camera, on nearly every map except the Nexus and the Shrine of Storms.
  - **The two symptoms were the two cubemap lookups.** EnvDif is sampled by the surface normal, so a corrupt cubemap paints static speckle onto the material; EnvSpc is sampled by the reflection vector, so the same corruption swims with the camera. Recognising that narrowed it to the cubemaps before any decode was inspected.
  - **The maps split perfectly along cubemap format.** Block-compressed DXT1: m01, m03, m05, m07. Uncompressed (`fmt=10`, no fourCC): m02, m04, m06, m08, m99. **The two maps reported as fine are both DXT1, and m02 - reported as one of the worst - is uncompressed.** A one-shot survey of the `_9999.tpf` cubemap layouts made the correlation obvious.
  - **The bug: `Pfim` reports an uncompressed ARGB8888 face as `Rgb24` and reads 3 bytes per pixel out of 4-byte data.** Every pixel is misaligned by one byte more than the last, so the channels rotate on a four-pixel cycle. Real m02 payload `FF 5D 5D 5D | FF 5F 5F 5F | FF 61 61 61` decodes to `(93,93,255) (95,255,93) (255,95,95) (97,97,97)` - blue, green, red, grey, repeating. **A literal RGB rainbow with a four-pixel period.** Per-pixel `|max-min|` across RGB: **158 mean / 244 max**, against **3.1** for a correctly-decoded m01 face.
  - **It decoded without any error, and an earlier revision of this project wrote the misread down as a feature** - `DecodeFaceImage`'s comment said cubemap faces "come back from Pfim in two different formats in this game's real data (Rgba32 for m01's compressed cubemaps, Rgb24 for m02's uncompressed ones)". That was Pfim being wrong, accepted as data. **When a decoder reports an unexpected format, check the byte budget before accommodating it**: 32x32 Rgb24 needs 3072 bytes and the face stride was 4096, which is 4.00 bytes per pixel exactly - the arithmetic said "4-byte pixels" all along.
  - **Fix: uncompressed faces bypass Pfim entirely**, which they should have from the start since there is nothing to decompress. `IsBlockCompressed` reads the DDS pixel-format `dwFlags` DDPF_FOURCC bit (offset 80); raw faces are read straight out of the payload as A,R,G,B. **The byte order is confirmed, not assumed**: every pixel's first byte is a constant `0xFF` while the other three vary together, so the first byte is alpha; reading it as B,G,R,A would make every pixel maximally blue. Only the base level is taken and Godot regenerates mips - the real strides confirm that layout (4096 = 32x32x4 with no mips; 5460 and 21844 are 32x32 and 64x64 with a full chain).
  - **Verified across all nine maps by rendering, since cubemaps can't be read back under Compatibility.** Sampling each map's `EnvDif_<map>_000` through a real `samplerCube` and measuring per-pixel channel divergence: the five uncompressed maps now read **0.000** (correctly neutral) and the four block-compressed ones are unchanged at 0.002-0.008.
  - **Scope note for anything concluded earlier this session:** this shipped with the cubemaps in part 17, so every visual judgement made on an uncompressed-cubemap map since then was made through it. The Nexus work is unaffected (m01 is DXT1), but any m02 observation from that window is suspect.

- **Part 25 — the props were black because of a gap left open on a miscalculated risk, not because of anything in the HemDir3 work (2026-08-28).** The user's own theory (that props needed to be unshaded the way map pieces are) was right in effect; enumerating the routing confirmed it and named the exact population.
  - **All 60 of m01's remaining `StandardMaterial3D` meshes were `g_LightingType=3` with no lightmap** (53 `Dif`, 4 `DifBmp`, 3 `DifSpcBmp`) - a case deliberately left on the old path one session earlier out of a worry that an ungated `env_intensity` would blow out. **Only 4 of m01's meshes are `g_LightingType=1` at all**, so the HemDir3 work, though correct, was never going to fix the reported symptom. Routing by lighting type without checking the population's *size* is what made that look like a fix.
  - **The risk that justified leaving it open didn't survive arithmetic.** With m01's real values a no-lightmap surface lands at a final 0.32 against a lightmapped one's 0.14 - 2.3x brighter, not blown. **And the engine agrees with the white default**: its gate is `min(shadow, lightmap)`, so a material shipping no lightmap is gated by the shadow term alone, which is ~1 wherever nothing shadows it. The "safe" choice was the inaccurate one, and it cost a full test cycle.
  - **Result on a real load: m01 goes from 335 HemEnv + 184 StandardMaterial3D to 518 HemEnv + 75 HemDir3 + 1 StandardMaterial3D**; m02 to 1651 + 37 + 1. The single survivor per map is the genuinely unlit (`g_LightingType=0`) sky dome, which belongs there.
  - **Lesson worth keeping: when deferring a case as "risky", cost the risk before deferring.** Both this and the part-22 fitting failure came from acting on an unquantified worry - one by fitting around it, one by routing around it - where a few lines of arithmetic would have settled it either way.

- **Part 26 — `DS_Fil_HDR` is five instructions and does no arithmetic at all (2026-08-28), which invalidates the reasoning behind the part-23 contrast change and reframes the "dark areas" problem entirely.** Found by grepping the RPCS3 capture for `sampler1D`, which returns exactly three programs, all byte-identical in body.
  - **The whole shader is:** `r0 = TEX2D(0, tc0.xy);` then `r0.z = TEX1D(1, r0.z).z;` / `.y` / `.x`. Sample the scene, three per-channel 1D lookups, done. **No pivot, no pow, no brightness multiply, no contrast, no saturation, no hue.** Every one of `grayKeyValue`, `brightnessRGB` and `contrastRGB` is baked into the lookup table CPU-side, where no capture can see it. Saturation and hue are cross-channel and cannot fold into a per-channel 1D LUT, so `DS_Fil_HDR_ColAdj`'s extra ~9 instructions over `DS_Fil_HDR` are presumably those.
  - **This means neither of this project's two contrast formulas was ever engine-accurate.** The original `(x - 0.5) * contrast + 0.5` was written from convention. Its replacement `pow(x, contrast)` was justified by "`ColAdj` contains no float literals, so the pivot form is ruled out" — a true premise supporting an invalid conclusion, since the absence of literals is explained by the LUT and constrains nothing about curve *shape*. **A third distinct failure mode this session, after part 22's fitting and part 21's textbook-formula substitution: treating a real observation as proof of a mechanism it did not actually constrain.**
  - **The power form was kept at this point, on a narrower and honest basis:** the pivot form is demonstrably harmful (it clamps real scene values to zero — arithmetic, not preference), while `pow` is monotonic, maps 0 to 0, and is an exact identity at the default contrast of 1.0. It is an unverified stand-in for an unrecoverable baked curve, not a finding. (Reverted to pivot in part 33 — the engine-accurate haze now lifts shadowed geometry clear of the pivot black point, which is the condition it always needed.)
  - **The user's decisive observation, and the reason this matters: back when fog was visible at player range, the fog itself lifted dark areas enough to reveal wall engravings, closely matching the emulator.** In-scatter is additive (`result * tc8 + tc9`), so it lifts shadowed regions without touching lit ones — which is exactly the reported behaviour. **So "shadowed geometry too dark" was a missing-atmosphere symptom all along**, and the part-23 contrast change was treating it downstream of the real cause. Fixing the scattering is the fix; do not reach for the tone curve for this again.
  - **Structural consequence worth acting on eventually:** the engine applies tonemap *and* per-channel brightness/contrast as a **single** per-channel transfer function, not as a tonemap followed by a separate contrast operation. This project models them as two sequential steps. That is a shape difference, not just a parameter difference, and it is the honest description of what `des_tonemap` + `des_tone_correct` are standing in for.

- **Part 29 — the `/betaTotal` divide was never in the real shader, and it was the actual reason the atmosphere couldn't be boosted into visibility (2026-08-28).** Found by directly answering the user's question - "what made the earlier version strong enough at player range that this one isn't, no matter how much we scale it" - which pointed straight at a ceiling, not a distance problem.
  - **The divide is gone. This is a correction of a self-contradiction, not a new guess.** Part 27 added `/betaTotal` on a "closed-form integral" argument (`(1-Fex)` and the divide come from the same integral, so if the shader has one it has the other) and never checked that argument against the instruction stream. It's wrong: `VertexProgram1.spirv` multiplies the phase-weighted sum straight through by `(1-Fex)` and four more constants with **no DIV/RCP instruction anywhere in the chain**. `FOG_DIG_FINDINGS.md` §1.3 already had this exact divide-free formula written down from an independent re-trace - the divide was added to the shader anyway, and the two were never cross-checked against each other. **A fourth distinct reasoning failure this investigation has now caught in itself**, after part 21 (textbook substitution for a trace gap), part 22 (fitting one parameter against a different parameter's observable), and part 23/26 (a real observation proving a mechanism it didn't constrain): writing a justification without checking it against evidence already in hand.
  - **Why this explains the symptom exactly, not just approximately.** The divide caps the in-scatter's maximum possible colour (as `d -> infinity`) at B/R ~1.06 for m01's authored values - *below* the reference frame's measured 1.29-1.57 at every distance, because dividing a wavelength-dependent numerator by a wavelength-dependent denominator that's dominated by the same term cancels most of the channel difference back out. `scatter_distance_scale` only controls how fast the curve approaches its ceiling, not how high the ceiling is - so no amount of "boosting" that constant could ever have worked, which is exactly what two rounds of user testing (parts 27-28) independently found. Without the divide the ceiling is B/R ~3.26, comfortably above the reference range.
  - **`scatter_distance_scale` re-solved for the new (3-10x larger) magnitude: `4.0e-5`**, down from `6.73e-4` - every previous fit of this constant was tuned against the wrong ceiling and none of them carry over. Anchored the same way as part 28 (m01's own `FOG_BANK.fogEndZ = 220`, targeting a reference-matched B/R of 1.4 there): B/R climbs 1.06 at 30 units -> 1.29 at 150 -> 1.41 at 220 -> 1.71 at 400 on a neutral surface, which should read as present within normal movement range this time rather than only far outside the map.
  - **User-confirmed wrong, in a way that mattered: see part 30.** The prediction two lines up (that the ceiling was "now high enough") was itself wrong - the ceiling of B/R 3.26 this entry relied on was a symptom of unverified wavelength weights, not a correct target, and the real fix was to those weights, not to `scatter_sun_intensity`/`inscatteringMul` as guessed here.

- **Part 30 — the "no divide" fix above was itself wrong in a different direction, and the real lever was the wavelength weights, not the divide.** User test: removing the divide barely changed visibility and made the colour "much stronger blue which isn't right at all." Correctly refused another distance-scale boost and asked what made earlier versions strong at player range - the right question, and it led somewhere neither previously-tried extreme reached.
  - **The divide and hue-saturation questions are separable, and part 29 conflated them.** Dividing in-scatter by a per-channel `betaTotal` collapses hue toward neutral (ceiling B/R 1.06, part 27). Not dividing keeps the raw ratio between the wavelength weights as the ceiling (B/R 3.26, confirmed to match `RAYLEIGH_WAVELENGTH_WEIGHTS`' own ratio almost exactly - the small gap is Mie dilution). **A uniform divide (by any channel-independent value, including the scalar mean tried in part 27) cannot change the ratio between channels at all - it only rescales brightness.** So the saturation ceiling was never actually a divide question; it's set almost entirely by the wavelength weights, which have been an unchecked textbook import (lambda^-4 at 650/570/475nm) since the scattering was first implemented.
  - **`RAYLEIGH_WAVELENGTH_WEIGHTS` changed from lambda^-4 `(0.479, 0.810, 1.681)` to lambda^-1.5 `(0.786, 0.957, 1.258)`.** Solved by matching the ceiling to the reference frame's own measured maximum (B/R ~1.57, from `FOG_DIG_FINDINGS.md` §1.8): lambda^-4 gives ceiling B/R 3.26, lambda^-1.5 gives 1.56. This is a hypothesis fit to one number, not a derivation - there's no physical justification for the exponent 1.5 specifically, only that it's the one that lands on something checkable.
  - **`scatter_distance_scale` was deliberately left untouched** at `4.0e-5`, even though it's now known-stale (it was anchored against the old, too-saturated ceiling, so the new weights alone will make the onset noticeably weaker than what was just tested - B/R at m01's `fogEndZ` drops from ~1.41 to ~1.15 at the same scale). Left alone so the hue fix and the still-open visibility problem stay attributable to separate tests, rather than repeating the bundling that made earlier rounds hard to read. **Expect the user's "not enough for a player to see" complaint to still stand after this change** - that is the next thing to fix, once the hue is confirmed.
  - **User-confirmed 2026-08-28: hue is right ("that more accurate blue"), visibility is unchanged - see part 31.**

- **Part 31 - the real space is much smaller than the raw `m01` FLVER bounding box suggested, which had made `FOG_BANK.fogEndZ = 220` look like it might be an unreachable sightline; it isn't, and that finally gave a real anchor for `scatter_distance_scale`.** Measuring each map piece's own FLVER header bounding box (not MSB placement transforms, which are all-zero for map pieces - their vertices are already world-space) put `m01`'s aggregate extent at ~3762x5468x3762 units. Character models (`c9997`/`c9999`) confirmed 1 unit = 1 metre first (height 1.73-1.76), so a kilometres-wide block looked like it might mean `fogEndZ`'s "220" was nowhere near what a player actually sees. **User correction: `m01` also contains the entire Old One endgame area, a separate landscape reached by a long shaft below the Nexus** - two of the block's FLVER pieces are a nearly-flat 1.4-unit-tall, 1.4km-footprint plane, unmistakably that outdoor terrain, not anything in the Nexus. The pieces clustered near the origin (plausibly the Nexus chamber itself, by elimination against the landscape's obviously different flat/huge shape) run 170-330 units - comfortably matching `fogEndZ`. So 220 was a reasonable anchor all along; the aggregate measurement was just contaminated by an unrelated area, and per-piece bounding boxes are the right level to measure at, not the whole block.
  - **The real fix wasn't a bigger number - it was noticing the ratio's peak is independent of the distance scale entirely.** Because every channel's extinction rate scales by the same `distance_scale`, rescaling distance only moves *where* the R/G/B ratio peaks along the distance axis (the channels approach their different ceilings at different rates, so the ratio overshoots the true infinite-distance ceiling before settling back - which also explains the reference frame's own non-monotonic reading, mid-distance sample 1.57 > far sample 1.39, previously unremarked-on) - never *how high* it peaks (1.78 for a neutral surface, at any scale). Every previous fit (`2.5e-5`, `1.66e-4`, `6.73e-4`, `4.0e-5`) solved for a specific ratio *value* at a chosen distance - a strictly harder, more fragile target than just placing an already-adequate peak at the right place.
  - **`scatter_distance_scale` set to `9.5e-4`**, placing the peak (B/R 1.78, close to the reference's own 1.57) at ~220 units - the room's own real scale. Predicted curve on a neutral surface: B/R 1.37 at 30 units, 1.69 at 100, 1.78 at 220, easing to 1.74 by 376 (the largest plausible Nexus piece's own diagonal).
  - **User-confirmed 2026-08-28: "almost 1:1 now" against a real RPCS3 screenshot.** The atmosphere work (parts 20-31, twelve entries) is closed as engine-accurate. Remaining reported gap: the map's own lighting/shading reads too neutral/white against RPCS3's warmer tone - a different subsystem (`LIGHT_BANK`, not the atmosphere), see part 32.

- **Part 33 - the `LIGHT_DIG` investigation, and the atmosphere/output-stage state it landed (2026-08-28). Explicitly not claimed solved: "about the closest we can get it for now, there's still something missing" (user), to be revisited once VFX and bloom are in and there is a full pipeline to judge against.**
  - **The `/betaTotal` divide was reopened and re-added, this time as a *scalar*.** Parts 29-30 removed it because `VertexProgram1.spirv` has no `DIV`/`RCP` in the scattering chain. Re-tracing the bytecode line by line for `LIGHT_DIG` showed that premise is true but the conclusion does not follow: the phase functions arrive **raw** (`(1+cos^2 theta)` with no `3/16pi`; `denom^-1.5` scaled only by `c107.x`), so every normalizer, both betas, the wavelength split **and any divide** are folded into the CPU-supplied `c108`/`c109` constants, invisible to any capture. Absence of the *instruction* is not absence of the *operation* - the same shape as part 21's failure mode, which the `LIGHT_DIG` brief itself listed first. `des_scatter()` now divides the phase-weighted in-scatter sum by `max(scatter_beta_ray + scatter_beta_mie, 1e-4)` - the **scalar** authored coefficients, i.e. H&P's own normalization with `lsBetaRay`/`lsBetaMie` taken as scalars.
  - **Scalar, not per-channel - that distinction is the whole reason part 27's rejected `/betaTotal` does not transfer.** `lsBetaRay` is authored as one float; the wavelength split is applied after. Dividing by the per-channel `betaTotal` vector cancels the split back out (ceiling B/R ~1.06 - part 27's measured result, reproduced exactly). Dividing by the scalar cannot move any channel ratio at all (part 30's own lemma), so the lambda^-1.5 ceiling hue parts 30/31 fitted and the user confirmed survives untouched.
  - **Three independent measurements said a ~1/6 uniform gain was missing**, none depending on distance or `scatter_distance_scale`: (1) inverting `nexusrpcs3.png` through this project's own output stage recovers a brightest-haze radiance of R~0.18 / B~0.27, versus the undivided m01 ceiling of (0.60,0.72,0.94) at cos 0 up to (1.43,1.65,2.04) at cos 0.9 - 4-8x too high; (2) undivided saturated in-scatter spans 1600x across the ten shipped drawparam sets (m07 0.001, m99 1.61), versus 32x divided, with 7 of 10 then landing in the 0.07-0.30 scene-radiance band; (3) the pre-divide near field went flat several screen rows deep - an additive term saturating before the frame starts.
  - **`scatter_distance_scale` 9.5e-4 -> 3.5e-3.** Part 31's value placed a *colour-ratio peak* at ~220 units; a scalar divide makes the ceiling hue depth-flat, so there is no peak left to place. The value now only has to bring `(1 - Fex)` to ~0.9 by the back of the visible room (`m01_dofbank` row 0 `神殿` authors a 150 m far-DOF; `fogEndZ` is 220; per-piece FLVER boxes run 170-330). At 9.5e-4 the divided in-scatter only reached ~15% of its now-modest ceiling mid-room and the atmosphere read as a weak flat veil (user-confirmed). A controlled lit/shadow wall crop at ~3.5e-3 then measured shadow-half B/R 1.58 vs RPCS3 1.62, and far-band 1.40 vs 1.44 - the cool haze on dark and distant surfaces matches. `dofbank`/`shadowbank`/`lensflareexbank` were confirmed never read and carry authored `[m]` distances; `envlighttexbank` is byte-identical across all ten maps and entirely at paramdef defaults (dead data); the `sXX_lightbank` series has row names matching `mXX` 1:1 and is an alternate authoring pass, not tendency lighting and not a distance source.
  - **FOG_DIG's fog-selector count refined:** re-grepping the corpus with a broader match, 7 of 183 tc8-carrying fragment programs consume the `FOG_BANK` ramp (part 28's "6 of 180" undercounted) - 5 diffuse-only/unlit plus 2 with a tex1/tex2/cubemap-unit-8 signature. Confirms part 28's decision rather than merely permitting it.
  - **`des_tone_correct` contrast: the LIGHT_DIG pass recommended pivot -> `pow` again; reverted to the pivot form at the user's direction.** This restores the **part-19 output stage** (pivot contrast, no diffuse-hemisphere term), which the user recalls as the closest map-piece lighting *colour* to RPCS3 - pivot brightens above 0.5 where `pow` only ever darkens, and that upper-range lift reads as the warmer, less washed tone. The standing part-23/26 objection (pivot's ~0.027 black point crushes ambient-only-lit geometry) assumed nothing else lifts the shadows; the now-engine-accurate scattering haze is what does, which is the condition the pivot form always needed. **The part-19 -> part-23 revert was made on a misread the user has since named:** the shadow-detail loss attributed to the tone curve was actually the then-inaccurate fog being too weak and mis-coloured to lift the wall engravings - a last-moment observation not spoken up on at the time.
  - **The diffuse-hemisphere term (`colA_du`/`colA_dd`) stays removed** - part-19 composition. Parts 15 and 32 wired it three times (flat x2, then lightmap-gated) and every one flattened the Nexus or washed it to beige; the map's warmth comes from `TONE_CORRECT_BANK` and the lightmap, not this pair.
  - **User verdict, and what is explicitly not closed.** "About the closest we can get it for now. There's still something missing, but we probably won't be able to nail it down until after more VFX and bloom." The map-piece lighting/coloration may now be *more* accurate than the fog; the fog is the easier of the two to revisit. Sufficient for now - **not** "engine-accurate solved". The scattering model rests on one `[INFERRED]` normalization (the scalar denominator - corroborated by the three measurements above, not by the bytecode), one fitted wavelength exponent (lambda^-1.5, fit to one reference number), and one observational distance scale - none independently verifiable until there is a full graphics pipeline to judge against.

- **Part 32 - `LIGHT_BANK`'s "diffuse hemisphere" term (`ディフューズ上/下半球`), first found and reverted in part 15, re-introduced now that its stated blocker is resolved.** Re-surfaced by dumping every field on a live `m01` row, prompted by the user's "too white/cool vs RPCS3's warmer" report - and initially implemented from scratch (gated by the lightmap) before catching, mid-session, that this project already has a full history with this exact field that flatly contradicted that approach. **Caught before user testing, not after** - worth recording as a near-miss: `docs/ARCHITECTURE.md`'s own `LIGHT_BANK` entry already said "wired 2026-08-27 and reverted the same day", in a line describing the *ambient* term's neighbours, three paragraphs before this session went looking. A full-file check for prior art on the exact field name would have caught this before writing a single line of shader, not after.
  - **The full part-15 history, since it's the reason this implementation looks the way it does.** `LIGHT_BANK` carries two hemisphere pairs; only "ambient" (`u`/`d`) had ever been read. The unread "diffuse" pair (`du`/`dd`) is 5-30x larger on real rows (Nexus row 0: ambient 0.05/0.07 vs diffuse 1.60/1.44). Added flat, summed with ambient - it **flattened the Nexus** (user-confirmed against a real RPCS3 screenshot: shadowed areas disappeared, the scene read uniformly bright). Root-caused as **clipping, not a wrong formula**: this project had no transfer function at all at the time (`ALBEDO` fed straight from the sum), and `m01` row 0's numbers already had the lightmap's top ~35% clipping before this term existed - adding a constant ~1.6 pushed most of the frame past 1.0. Two reformulations were tried and independently disproven against the param corpus before reverting - **lightmap-gating the pair** (105 of 186 referenced rows have `colA_du=0` and would collapse toward black under a gate multiplying the *whole* hemisphere sum, not just this term) and **treating it as a substitute for the directional trio** (Pearson r = -0.15 between their totals, no relationship). The conclusion, corrected after user review: *"the pair should return once a transfer function exists"* - not "add something else" or "revert and move on."
  - **That transfer function now exists.** `des_tonemap()`/`des_tone_correct()` (`output_stage.gdshaderinc`) shipped across this session's atmosphere work (parts 18, 20-31) - a Reinhard-shaped compression plus per-channel brightness/contrast, exactly the missing piece part 15 named. So this is **the same flat-additive formula part 15 already validated as correct in shape**, re-added for the reason part 15 already gave, not a fresh guess: `diffuse_hemi(normal)`, same hemisphere shape as `hemisphere_ambient()`, summed alongside it (un-gated) in both the `HemEnv` and `HemDir3` branches of `lightmap_common.gdshaderinc` and in `terrain_blend.gdshader`.
  - **User-confirmed regression, and it predicted its own failure mode without anyone noticing at the time.** "The entire Nexus is now a flat mix of ... pastel beiges and toothpaste colors, almost no shading at all ... not blindingly bright, but not dark anywhere." A *different* symptom from part 15's ("uniformly bright", clipping to white), which briefly looked like a new bug rather than the same one - it isn't. **The transfer function existing did not fix the actual problem, because the actual problem was never clipping - it was the shadow/light ratio.** This engine's visible contrast comes from the ratio between a small shadow floor and a much larger lit floor, not from either one's absolute value. Adding a large flat constant to *both* compresses that ratio toward 1 regardless of what happens downstream: `m01`'s real numbers put the shadow floor at 0.06 and a plausible lit floor around 1.06 (~18x) before this term; both plus the flat diffuse-hemi term land around 1.58 and 2.58 (~1.6x) after. A tonemap changes how an over-bright *result* gets squashed (soft desaturating compression instead of a hard clip), which is exactly why the symptom looks different - pastel wash instead of blown white - but it does nothing about a ratio that was already destroyed further upstream.
  - **The fix: gate this term by the lightmap - not a re-run of part 15's disproven gating.** Part 15's gating attempt multiplied the *entire* hemisphere sum (ambient + diffuse) by the lightmap, which would zero out even the always-on ambient term in deep shadow - correctly rejected. Gating *only the new term* doesn't have that failure mode: on the 105/186 rows where its intensity is authored at `0%`, the term is already zero with or without a gate, so part 15's specific rejection reasoning doesn't transfer to this narrower change. This restores the same contrast-preserving mechanism `env_diffuse` already relies on. `diffuse_hemi(normal, lightmap)`, gated, was shipped at this point — **then removed entirely in the part-19 restoration (part 33); the term is not read now.**
  - **User-confirmed partial fix (2026-08-28): the gate worked, the colour is still wrong.** "There's a lot more contrast now, the shadows having a real effect again, but everything is still this very flat beige, still very wrong." So the contrast diagnosis in this entry was correct and is **closed** - the shadow/lit ratio mechanism, and gating the new term instead of the whole sum, both hold up under test. What was left at the time - the scene reading as a flat uniform beige - drove the part-19 restoration and part 33: the diffuse-hemi term was removed, the pivot contrast form restored, and the residual colour gap deferred until the graphics pipeline (VFX, bloom) is further along. See part 33 and `.local-notes.md`.

- **Part 28 — the FOG_DIG investigation's proposal implemented: `FOG_BANK`'s lerp dropped from `des_output`, scattering re-enabled with the per-channel `/betaTotal` divide (2026-08-28).** Not yet user-confirmed in-editor - this entry records what changed and why, pending visual review.
  - **What changed:** `des_output` no longer calls `des_fog()`. It now runs `des_scatter()` (per-channel-divide form) directly on the shaded colour, then tonemap/tone-correct. `scatter_distance_scale` is set to `1.66e-4` - the one candidate that was ever actually *solved* (a two-band fit against the reference's mid/far B/R), previously rejected while FOG_BANK's fog was still running underneath it on every surface.
  - **Why fog was dropped rather than fixed:** the FOG_DIG pass found the real fragment-program corpus applies the `FOG_BANK` lerp in only 6 of 180 programs that carry the scattering (refined to 7 of 183 in part 33) - a per-material exception, not the blanket per-pixel lerp this project had been applying to literally every map surface. Which specific materials get it isn't recoverable without naming the compiled shaders, so applying it nowhere is the defensible default until that's known.
  - **Why the per-channel divide's earlier rejection doesn't count against it this time:** it was tested once before (part 27) and rejected for producing a near-neutral in-scatter that read as "FOG_BANK's violet wins" - but at the time FOG_BANK's violet lerp was unconditionally applied on top of it. A near-neutral term sitting under an always-on violet lerp will always look like the violet won; that test never isolated the divide.
  - **The measurement behind this:** the FOG_DIG pass found the reference frame lifts green and blue *together* at every depth (G/R 1.19-1.27, B/R 1.31-1.57) - Rayleigh-shaped - while the previous Godot output lifted blue alone with green flat, which is exactly `FOG_BANK`'s `(54,50,120)` signature (green sits below red in that colour). No tuning of a lerp toward that colour can produce the reference's tint; the scattering was always the better candidate for it.
  - **What would falsify this:** if the Nexus still reads violet or grey rather than picking up a light blue-green haze, the in-scatter is not the source of the reference's tint after all, and the fog lerp's colour must be getting transformed somewhere between the bank and its use - see the findings doc's open question 1 (naming the compiled shaders) as the next step in that case.
  - **Build/compile status:** `dotnet build` clean, and all six `des_output` consumers (`lightmap`/`_alpha`/`_add`/`_sub`, `terrain_blend`, `water`) compile with zero `SHADER ERROR` under the real OpenGL3 driver.
  - **User-confirmed, in part: the hue reversal worked, the distance did not.** Flying around m01 at normal player range showed no visible atmosphere at all - "as they did without any of it"; the light blue-green tint only appeared, faintly, from far outside the map. **This is the win the whole FOG_DIG pass was aimed at**: the colour is finally reading right (light blue-green, not violet), confirming both the divide and dropping the blanket fog lerp. What's left is a single scalar. Half-extinction at the original `1.66e-4` was 564 (blue) to 1734 (red) units - far outside any distance a player normally travels within one map.
  - **`scatter_distance_scale` re-anchored to `6.73e-4`** (~4x), solved against `FOG_BANK.fogEndZ = 220` - m01's own authored "how far this area's atmosphere reaches" number (the paramdef field is literally labelled in metres), repurposed as a spatial scale now that the colour term it used to carry is gone. Red (weakest channel) hits 30% attenuation by 220 units; blue (strongest) reaches 50% by ~139, comfortably inside normal movement range. **Not yet user-confirmed** - this is a reasoned re-anchor, not a second independent measurement, and the assumption that the removed linear fog and the scattering were tuned to read as "there" over comparable distances is plausible but unverified.

- **Part 27 — three more scattering compositions built and all three user-rejected (2026-08-28). The scattering is now DISABLED and only `FOG_BANK`'s linear fog runs.** This entry is a record of what has been eliminated, not of a fix. Nothing here should be re-tried without new information.
  - **The three normalizations, on `m01`'s row 0, in-scatter linear at `cos_theta = 0`:**

    | form | in-scatter | B/R | user verdict |
    |---|---|---|---|
    | no divide | `(0.38, 0.62, 1.24)` | 3.25 | clips to saturated blue — rejected |
    | `/ betaTotal` per channel | `(0.110, 0.113, 0.116)` | 1.06 | neutral, `FOG_BANK` violet dominates — rejected |
    | `/ mean(betaTotal)` | `(0.058, 0.095, 0.190)` | 3.25 | still violet, range still wrong — rejected |

  - **The per-channel divide has the soundest derivation, and that is exactly why its rejection is informative.** In-scatter along a ray is `∫ β·P·Esun·exp(-β·t) dt`, closed form `(β·P/βTotal)·Esun·(1-Fex)`; the `(1-Fex)` factor the shader provably has (`c466.w = 1.0`) comes out of that same integral, so the divide should be there. Shipping it produced a *worse* picture than shipping no atmosphere at all. **Conclusion: the error is not in the divide. Something upstream of it is wrong** — most likely what `FOG_BANK` is for, or what `c104` actually contains, or whether this term reaches map materials at all.
  - **`scatter_distance_scale` was tried at 2.5e-5, 1.66e-4 and 6.5e-4 and rejected at every value.** Note that the middle one was a genuine two-band solve, over-determined and insensitive to the surface-brightness assumption, and it still looked wrong. **A parameter that fails across a 26x range is not a mis-tuned parameter; the model around it is wrong.** It now sits at a placeholder `1.0` meaning "unknown", with the function disabled.
  - **The series-vs-crossfade composition question is likewise unresolved.** Part 20/21 crossfaded `FOG_BANK` against the scattering by `blendCoef`; part 27 ran them in series on the argument that `blendCoef`'s paramdef scopes it to the scattering alone. Both compositions produced a violet Nexus. Since the two exhaust the obvious options, the likelihood is that **`FOG_BANK`'s colour is not used as a fog target colour the way this project assumes it is.** `(54,50,120)` is a dark violet; the reference frame's haze is a bright desaturated steel-cyan, and no lerp toward `(54,50,120)` produces that from any surface colour.
  - **What the user's three independent observations jointly require, none of which any built form has satisfied at once:** haze that is *light blue*, that *reaches near geometry* (tens of units, not hundreds), and that *lifts shadowed surfaces* enough to reveal wall engravings. The third is the tell — a `mix()` toward a mid-value colour lifts darks *and* darkens lights, whereas the reference lifts darks while leaving lit stone alone. That is additive behaviour, which is what `result * tc8 + tc9` is, and it means the visible atmosphere is probably almost entirely the **in-scatter term**, with `FOG_BANK` doing something other than what this project currently does with it.
  - **Two leads discarded so they are not re-chased.** `m01_tonemapbank` binds correctly (64 rows, `grayKeyValue=0.18`, `minAdaptedLum=0.08` all reaching the shader) — an earlier claim that it returned null was a bug in a throwaway dump script choking on a `pad_0` byte-array cell, not in `DrawParamReader`. And the "shadowed geometry too dark" report is **not** a separate exposure defect: it is the missing in-scatter, as recorded in part 26.
  - **Not in any readable file, checked this session:** `other/*.properties` (leaderboard/SOS config), the 69 `paramdef/*.tdf` (Shift-JIS enum-label tables for the param editor), `other/envlight.tpf`/`lensflare.tpf` (texture banks). The `.vpo` literal pool is exhausted (`c466` plus vertex-attribute decode constants). `c104` is read as `.xyzx`, so it has three distinct components and is CPU-computed per draw; if it exists as data it is in `EBOOT.BIN`. RSX capture is separately ruled out on practical grounds.
## C#/Godot interop gotchas worth remembering

- `ShaderMaterial.SetShaderParameter()` from C#, for a shader uniform hinted
  `: source_color` (e.g. `uniform vec3 water_color : source_color`), needs a `Color`, not
  a `Vector3` — passing `Vector3` doesn't throw, it silently no-ops (the parameter reads
  back `null` via `get_shader_parameter()`, as if never set, while every other
  non-color-hinted uniform set the same way works fine). Found by inspecting an imported
  water material headlessly and noticing exactly the color-hinted uniforms were null.
- **A method whose signature uses a non-Variant type (a custom `record struct`, `PARAM.Row`, a
  nullable struct) is not exposed to GDScript at all** — calling it fails with `Invalid call.
  Nonexistent function '<name>' in base '<Class>.cs'`, not a marshalling error, so it reads like
  the method is missing. Since throwaway GDScript check scripts are this project's only
  verification path, a C# API meant to be *verified* has to return Variant-compatible types
  (`Godot.Collections.Dictionary`/`Array`, `string[]`, primitives). Found 2026-08-27 on
  `DrawParamReader.GetEnvCubemapNames`.
- **`ImageTextureLayered.get_layer_data()` (so `Cubemap`, `Texture2DArray`) returns blank images
  under the Compatibility renderer** — not null, not an error, just zeros, so it reads as "the
  texture is empty" rather than "readback is unsupported". Verified with a control built from
  known solid colours in pure GDScript. To check a layered texture's real contents, render with
  it and read the *viewport* image instead. Found 2026-08-27 while verifying env cubemaps.
- `ProjectSettings.GlobalizePath()` is a safe no-op on paths that are already absolute
  filesystem paths (not `res://`), so it's fine to call it even when unsure whether the
  path Godot hands you is virtual or already global.
- `Image.CreateFromData`'s `useMipmaps` parameter means "this buffer already contains a
  full mip chain" — not "please generate mips for me." Pass `false` and call
  `image.GenerateMipmaps()` yourself, or the base-level-only buffer gets rejected/
  misread.
- `StandardMaterial3D.Metallic` defaults to `0.0` and *multiplies* against
  `MetallicTexture` — setting the texture alone has zero visible effect without also
  setting `Metallic = 1.0f`.
- `MetallicTextureChannel` defaults to reading only the texture's Red channel. A true
  full-RGB spec map's G/B data is silently dropped. Known, accepted ceiling — not
  chased further since there's no PBR-correct slot for a Blinn-Phong spec map anyway
  without a custom shader.
- A `CSharpScript.new()` call can transiently fail (`Invalid call. Nonexistent function
  'new' in base 'CSharpScript'`) for the *first* headless invocation immediately after a
  project directory rename — Godot's own editor-side "is the C# assembly ready" tracking
  seems to be keyed off the old path. A `dotnet build` from the new location plus one
  throwaway import pass resolves it; it isn't a real code regression when it happens
  exactly once right after a move.
## Environment specifics (this machine)

- **Host moved from Arch Linux to Void Linux (glibc), 2026-08-21.** `godot-mono` and
  the .NET 8 SDK are now installed via the official direct downloads (godotengine.org,
  dotnet.microsoft.com) rather than a distro package, since Void doesn't package either
  the way Arch's `extra` repo did. Confirmed working end-to-end post-move: importer
  runs fine, no `DOTNET_ROOT` override needed. Every Arch-specific instruction below
  (package names, `pacman` invocations) is now historical — kept for the reasoning
  trail, not as current setup steps.
- **Net8 runtime: no special setup needed on Arch (superseded 2026-07-20, machine
  itself later superseded 2026-08-21 — see above).** Godot 4.7's `GodotTools.dll` (the
  "build solutions" feature) hardcodes a `net8.0` target with no cross-major
  `rollForward`, but `dotnet-sdk-8.0` was in Arch's own `extra` repo and installed
  cleanly alongside newer SDKs (`pacman -S dotnet-sdk-8.0`), no `DOTNET_ROOT` override
  needed — confirmed by running both `dotnet build Soulbrandt.csproj` and a full
  headless import with `DOTNET_ROOT` unset. An earlier revision of this doc wrongly
  assumed Arch didn't package a net8 SDK at all and used a manually-merged
  `~/.dotnet-godot` root as a workaround; that premise was wrong and the workaround is
  obsolete. If a `DOTNET_ROOT`/multi-SDK conflict ever resurfaces on the current (Void)
  or any future machine, merge the net8 install into one root with the existing system
  SDKs via symlinks rather than pointing `DOTNET_ROOT` at an isolated net8-only install
  (see the dead-ends entry below on why isolating it breaks SDK discovery).
- `godot-mono --headless --build-solutions` hangs indefinitely in this environment even
  with the runtime fixed, for reasons never fully root-caused beyond "something in
  Godot's own in-process MSBuild invocation." Given up on making it work; `Soulbrandt.csproj`
  is hand-authored instead, using the exact `Godot.NET.Sdk/4.7.0` + `net8.0` +
  `PackageReference`/`ProjectReference` shape Godot would have generated, and it builds
  fine via plain `dotnet build`.
- Running the GUI editor and any other headless `godot-mono` process against the same
  project simultaneously causes contention/hangs. Only ever run one Godot process against
  this project at a time. **Confirmed as a real OOM incident, not just a hang, on
  2026-07-21**: running a full headless reimport (from the since-removed reimport
  pipeline) while the GUI editor was also open froze the whole system for 1-2 minutes,
  then the kernel OOM-killer killed the GUI editor process (`journalctl -p err`: `Out of
  memory: Killed process ... (godot.linuxbsd.) total-vm:93855004kB,
  anon-rss:14996300kB`) — the headless process itself survived and finished normally.
  Close the GUI editor before starting any other headless `godot-mono` run (e.g. the
  `--rendering-driver` shader-compile check in docs/ARCHITECTURE.md's "Build & verify").

## Dead ends — don't re-try these

- Assuming Steam's Godot or "the latest official Godot" supports C# without switching
  builds — neither does; only the dedicated `.NET`/mono build does.
- Assuming an official/Steam Godot build has the `dds` module compiled in by default —
  checked empirically on two different builds, neither did. Moot now anyway since the
  native importer never relies on Godot's built-in DDS loader.
- Porting `Program.cs`'s OBJ-export winding swap or V-flip into the native importer "to
  be safe" — both are specifically wrong there (see above). If either bug's symptom
  reappears (inside-out geometry, misaligned textures), the fix is to *remove* a
  compensation, not add one back.
- Isolating `DOTNET_ROOT` to just the newly-installed net8 runtime — breaks SDK
  discovery. Always merge into one root instead of pointing at a narrow one.
- Treating an empty `~/godot/Boletaria/` directory (containing only a stray `.claude/`
  folder) as meaningful after the Soulbrandt rename — it was a harness cwd-recovery
  artifact from a stale path reference, not anything intentional. Already deleted; if
  something similar reappears after a future rename, it's the same harmless mechanism.
- Re-registering an `EditorSceneFormatImporter` for `.flver` to try to fix a reimport-concurrency problem "properly" instead of avoiding the pipeline entirely — already tried at length (self-throttling, engine-source-level investigation of every available hook) and confirmed Godot exposes no lever for this at all; see "Real bugs found and fixed" above for the summary and why the fix ended up architectural instead.
- Applying `DrSwizzler`'s PS3 deswizzle unconditionally to every texture format
  (matching Soulstruct's format-generic approach, including DXT1/BC1) to fix the
  lightmap block-noise artifact — tested directly via a before/after visual dump on a
  real lightmap texture; makes it *worse*, not better. `Headerizer.Headerize`'s own doc
  comment already says DeS/DS1 don't use PS3 swizzling for most formats; only keep the
  original narrow uncompressed-only condition. See docs/context.md's "Lightmap/drawparam
  system, part 9".
- A custom in-editor `Tree`-based file browser for selecting `.flver` files to load
  (`mounted_browser_dock.gd`, deleted 2026-07-24). The handler logic was verified
  correct in isolation (direct function-call simulation against real `mounted/chr/`
  data populated all rows correctly), but the real Tree's expand arrows never worked
  in the live editor — survived a plugin toggle and a full editor restart, symptom
  specifically "arrow disappears, nothing else happens." Root cause never isolated
  (see "Real bugs found and fixed"'s pivot entry for the full story); abandoned in
  favor of reusing the already-proven `EditorFileDialog` pattern instead of
  continuing to debug a from-scratch `Tree` blind. If a custom `Tree` browser is ever
  wanted again, budget time to actually diagnose the click→signal path in a real
  windowed session (not headless) before assuming the same approach will work this
  time.

## Deferred by design, not forgotten

See docs/ARCHITECTURE.md's "Known deferred work" for the current list (animation/skeleton import,
the metallic-channel ceiling, LOD z-fighting). The
in-editor asset-mounting UI (replacing the `mounted` symlink stopgap) is done - see
"Asset mounting system built" above. Its in-*game*/runtime counterpart (a shipped build
loading assets with no editor at all) is still not started, same as before.

Water shader visual-accuracy tuning (see the "Water surfaces and cross-category texture
reuse" entry above, and docs/ARCHITECTURE.md's water section) is explicitly paused by
the user, not stuck or abandoned - the
data pipeline (real per-material MTD params reaching the shader correctly) and the shader
logic itself (coloring, depth fade, tiling) are confirmed working and reasonably close
where they've been checked against real screenshots; what's left is a genuine environment/
lighting gap (no `WorldEnvironment` anywhere in the project, so testing has been against
the editor's own uncontrolled default preview), not something more shader iteration can
fix. Revisit once real lighting/environment/ambience work happens (see `docs/PLAN.md`).
The uncalibrated constants specifically (`refraction_scale`, `wave_detail_scale`,
`bump_smoose`'s applied weight) stay uncalibrated for a confirmed structural reason now,
not just "haven't gotten to it" - see "Lightmap/drawparam system, part 13" above: the
real shader binaries exist and were found and opened, but carry no parameter-name table,
so the actual numbers were never recoverable from game files at all, in this engine or
any other. Visual-reference calibration remains the only real path for these specific
constants.

A whole category of readable, currently-unextracted game data - `script/` (AI/dialogue,
272 plain-text `.lua` files + 61 `.esd` state machines), `menu/`, `sfx/`, `remo/`, `msg/`
- surfaced during the same search (see part 13 above). `SoulsFormatsNEXT` already has
working readers for all of it; none of it is wired into `AssetExtractor.KnownCategories`
or scoped into `docs/PLAN.md` yet. Real future value, independent of the shader work -
worth a deliberate scoping conversation rather than folding in ad hoc.
